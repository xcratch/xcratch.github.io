"use strict";
(self["webpackChunkGUI"] = self["webpackChunkGUI"] || []).push([["preload_https_3A_2F_2Fyokobond_2Egithub_2Eio_2Fxcx-costumex_2Fdist_2Fcostumex_2Emjs_extension_mjs"],{

/***/ "./preload/https%3A%2F%2Fyokobond%2Egithub%2Eio%2Fxcx-costumex%2Fdist%2Fcostumex%2Emjs/extension.mjs":
/*!***********************************************************************************************************!*\
  !*** ./preload/https%3A%2F%2Fyokobond%2Egithub%2Eio%2Fxcx-costumex%2Fdist%2Fcostumex%2Emjs/extension.mjs ***!
  \***********************************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   blockClass: () => (/* binding */ ExtensionBlocks),
/* harmony export */   entry: () => (/* binding */ entry)
/* harmony export */ });
var img$2 = "data:image/png;base64,";
var img$1 = "data:image/svg+xml,%3c%3fxml version='1.0' encoding='UTF-8' standalone='no'%3f%3e%3c!-- Created with Inkscape (http://www.inkscape.org/) --%3e%3csvg width='52' height='52' viewBox='0 0 13.758334 13.758333' version='1.1' id='svg1' xml:space='preserve' inkscape:version='1.3.2 (091e20e%2c 2023-11-25)' sodipodi:docname='inset-icon.svg' inkscape:export-filename='insert-icon.png' inkscape:export-xdpi='16.639999' inkscape:export-ydpi='16.639999' xmlns:inkscape='http://www.inkscape.org/namespaces/inkscape' xmlns:sodipodi='http://sodipodi.sourceforge.net/DTD/sodipodi-0.dtd' xmlns='http://www.w3.org/2000/svg' xmlns:svg='http://www.w3.org/2000/svg'%3e%3csodipodi:namedview id='namedview1' pagecolor='white' bordercolor='black' borderopacity='0.25' inkscape:showpageshadow='2' inkscape:pageopacity='0.0' inkscape:pagecheckerboard='true' inkscape:deskcolor='%23d1d1d1' inkscape:document-units='px' inkscape:zoom='7.7464162' inkscape:cx='29.239328' inkscape:cy='31.821167' inkscape:window-width='1816' inkscape:window-height='1144' inkscape:window-x='0' inkscape:window-y='43' inkscape:window-maximized='0' inkscape:current-layer='layer1' /%3e%3cdefs id='defs1'%3e%3cinkscape:path-effect effect='bspline' id='path-effect3' is_visible='true' lpeversion='1.3' weight='33.333333' steps='2' helper_size='0' apply_no_weight='true' apply_with_weight='true' only_selected='false' uniform='false' /%3e%3cinkscape:path-effect effect='bspline' id='path-effect2' is_visible='true' lpeversion='1.3' weight='33.333333' steps='2' helper_size='0' apply_no_weight='true' apply_with_weight='true' only_selected='false' uniform='false' /%3e%3cinkscape:path-effect effect='spiro' id='path-effect1' is_visible='true' lpeversion='1' /%3e%3c/defs%3e%3cg inkscape:label='Layer 1' inkscape:groupmode='layer' id='layer1' transform='translate(-99.306099%2c-62.206394)'%3e%3crect style='fill:none%3bfill-opacity:1%3bstroke:black%3bstroke-width:0%3bstroke-linecap:round%3bstroke-linejoin:round%3bstroke-dasharray:none' id='rect3' width='13.758333' height='13.758333' x='99.306099' y='62.206394' /%3e%3cg id='g4' transform='matrix(0.17333333%2c0%2c0%2c0.17333333%2c84.835702%2c40.6066)' style='fill:white%3bfill-opacity:1%3bstroke-width:3.05288471%3bstroke-dasharray:none'%3e%3cpath style='font-variation-settings:normal%3bopacity:1%3bfill:white%3bfill-opacity:1%3bstroke:%237733ff%3bstroke-width:3.05288471%3bstroke-linecap:round%3bstroke-linejoin:round%3bstroke-miterlimit:4%3bstroke-dasharray:none%3bstroke-dashoffset:0%3bstroke-opacity:1%3bstop-color:black%3bstop-opacity:1' d='m 111.01159%2c169.61411 c -2.692%2c-0.41416 -5.62865%2c0.26353 -7.43589%2c1.65662 -1.80725%2c1.39308 -2.48494%2c3.50145 -4.405131%2c4.04737 -1.920194%2c0.54592 -5.082686%2c-0.4706 -7.002881%2c0.16947 -1.920196%2c0.64006 -2.597872%2c2.93663 -1.261265%2c5.91106 1.336607%2c2.97442 4.68741%2c6.62642 9.054883%2c8.60306 4.367474%2c1.97664 9.751234%2c2.27782 13.493234%2c0.82826 3.74201%2c-1.44956 5.84183%2c-4.64976 6.4254%2c-7.90653 0.58358%2c-3.25678 -0.34911%2c-6.56987 -2.03913%2c-8.97953 -1.69002%2c-2.40965 -4.13723%2c-3.91563 -6.82922%2c-4.32978 z' id='path1' inkscape:path-effect='%23path-effect2' inkscape:original-d='m 111.25631%2c168.52225 c -2.93672%2c0.6777 -5.87337%2c1.35539 -8.81014%2c2.03311 -0.67772%2c2.10845 -1.35541%2c4.21682 -2.03311%2c6.32523 -3.162683%2c-1.01658 -6.325176%2c-2.03309 -9.487853%2c-3.04966 -0.677715%2c2.2967 -1.355392%2c4.59327 -2.033108%2c6.88998 3.350932%2c3.65214 6.701734%2c7.30414 10.0526%2c10.95621 5.384081%2c0.3012 10.767841%2c0.60239 16.151931%2c0.9036 2.09991%2c-3.20034 4.19974%2c-6.40053 6.29962%2c-9.6008 -0.93274%2c-3.31328 -1.86542%2c-6.62637 -2.79815%2c-9.93965 -2.44731%2c-1.50603 -4.89452%2c-3.01201 -7.34179%2c-4.51802 z' sodipodi:nodetypes='cccccccccc' /%3e%3cpath style='fill:white%3bfill-opacity:1%3bstroke:%237733ff%3bstroke-width:3.05288471%3bstroke-linecap:round%3bstroke-linejoin:round%3bstroke-dasharray:none%3bstroke-opacity:1' d='m 144.3326%2c136.36651 c -0.87998%2c-0.11962 -2.6399%2c-0.35887 -5.04401%2c0.37175 -2.40411%2c0.73062 -5.45216%2c2.43102 -9.30945%2c5.82655 -3.8573%2c3.39554 -8.52352%2c8.48595 -11.57173%2c12.2008 -3.04821%2c3.71486 -4.4784%2c6.05415 -5.19119%2c8.05425 -0.71278%2c2.0001 -0.70812%2c3.66085 0.47805%2c4.49544 1.18616%2c0.83458 3.55367%2c0.84292 5.00796%2c1.77226 1.45428%2c0.92934 1.99517%2c2.77958 2.74563%2c4.13076 0.75047%2c1.35119 1.71044%2c2.20316 3.55279%2c1.39846 1.84235%2c-0.80469 4.56686%2c-3.26595 7.67733%2c-7.08423 3.11048%2c-3.81827 6.60656%2c-8.99309 9.08215%2c-13.55568 2.47559%2c-4.5626 3.93039%2c-8.51241 4.33569%2c-11.3255 0.40531%2c-2.81309 -0.23893%2c-4.48916 -0.56106%2c-5.32721 -0.32213%2c-0.83806 -0.32218%2c-0.83802 -1.20216%2c-0.95765 z' id='path2' inkscape:path-effect='%23path-effect3' inkscape:original-d='m 145.21258%2c136.48614 c -1.75996%2c-0.23926 -3.51988%2c-0.4785 -5.27988%2c-0.71776 -3.04822%2c1.7005 -6.09627%2c3.40089 -9.1445%2c5.1014 -4.66636%2c5.09057 -9.33258%2c10.18098 -13.99881%2c15.2714 -1.43019%2c2.33929 -2.86038%2c4.67858 -4.29062%2c7.01794 0.005%2c1.66084 0.009%2c3.32159 0.014%2c4.98243 2.36766%2c0.008 4.73517%2c0.0167 7.10283%2c0.025 0.54092%2c1.85035 1.0818%2c3.70059 1.62271%2c5.55094 0.96002%2c0.85202 1.91999%2c1.70399 2.88002%2c2.55601 2.72467%2c-2.46141 5.44917%2c-4.92268 8.17385%2c-7.38409 3.49628%2c-5.17513 6.99235%2c-10.34995 10.48865%2c-15.52509 1.45489%2c-3.95005 2.90968%2c-7.89986 4.36457%2c-11.8499 -0.64427%2c-1.67614 -1.28851%2c-3.35221 -1.93277%2c-5.02832 z' sodipodi:nodetypes='ccccccccccccc' /%3e%3cpath id='path3' style='color:black%3bfill:white%3bfill-opacity:1%3bstroke:%237733ff%3bstroke-width:3.05288471%3bstroke-linecap:round%3bstroke-linejoin:round%3bstroke-dasharray:none%3bstroke-opacity:1' d='m 154.97753%2c174.7599 a 3.96875%2c3.96875 0 0 0 -5.61266%2c0 l -2.61303%2c2.61303 -2.61303%2c-2.61303 a 3.96875%2c3.96875 0 0 0 -5.61266%2c0 3.96875%2c3.96875 0 0 0 0%2c5.61266 l 2.61303%2c2.61303 -2.61303%2c2.61303 a 3.96875%2c3.96875 0 0 0 0%2c5.61266 3.96875%2c3.96875 0 0 0 5.61266%2c0 l 2.61303%2c-2.61303 2.61303%2c2.61303 a 3.96875%2c3.96875 0 0 0 5.61266%2c0 3.96875%2c3.96875 0 0 0 0%2c-5.61266 l -2.61303%2c-2.61303 2.61303%2c-2.61303 a 3.96875%2c3.96875 0 0 0 0%2c-5.61266 z' /%3e%3c/g%3e%3c/g%3e%3c/svg%3e";
var en$1 = {
  "costumex.entry.name": "CostumeX",
  "costumex.entry.description": "Costume extension"
};
var ja$1 = {
  "costumex.entry.name": "CostumeX",
  "costumex.entry.description": "コスチュームの拡張"
};
var translations$1 = {
  en: en$1,
  ja: ja$1,
  "ja-Hira": {
    "costumex.entry.name": "CostumeX",
    "costumex.entry.description": "コスチューム の かくちょう"
  }
};
var version$1 = "1.4.0";
var packageJson = {
  version: version$1
};

/**
 * This is an extension for Xcratch.
 */

/**
 * Formatter to translate the messages in this extension.
 * This will be replaced which is used in the React component.
 * @param {object} messageData - data for format-message
 * @returns {string} - translated message for the current locale
 */
var formatMessage$1 = function formatMessage(messageData) {
  return messageData.defaultMessage;
};
var version = packageJson.version;
var entry = {
  get name() {
    return formatMessage$1({
      id: 'costumex.entry.name',
      defaultMessage: 'CostumeX',
      description: 'name of the extension'
    });
  },
  extensionId: 'costumex',
  extensionURL: 'https://yokobond.github.io/xcx-costumex/dist/costumex.mjs',
  collaborator: 'Koji Yokokawa',
  iconURL: img$2,
  insetIconURL: img$1,
  get description() {
    return "".concat(formatMessage$1({
      defaultMessage: 'Costume extension',
      description: 'Description for this extension',
      id: 'costumex.entry.description'
    }), " (").concat(version, ")");
  },
  tags: ['image', 'costume'],
  featured: true,
  disabled: false,
  bluetoothRequired: false,
  internetConnectionRequired: false,
  helpLink: 'https://yokobond.github.io/xcx-costumex/',
  setFormatMessage: function setFormatMessage(formatter) {
    formatMessage$1 = formatter;
  },
  translationMap: translations$1
};
function _arrayWithHoles(arr) {
  if (Array.isArray(arr)) return arr;
}
function _iterableToArrayLimit(r, l) {
  var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
  if (null != t) {
    var e,
      n,
      i,
      u,
      a = [],
      f = true,
      o = false;
    try {
      if (i = (t = t.call(r)).next, 0 === l) ;else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
    } catch (r) {
      o = true, n = r;
    } finally {
      try {
        if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return;
      } finally {
        if (o) throw n;
      }
    }
    return a;
  }
}
function _arrayLikeToArray(arr, len) {
  if (len == null || len > arr.length) len = arr.length;
  for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i];
  return arr2;
}
function _unsupportedIterableToArray(o, minLen) {
  if (!o) return;
  if (typeof o === "string") return _arrayLikeToArray(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor) n = o.constructor.name;
  if (n === "Map" || n === "Set") return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
}
function _nonIterableRest() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _slicedToArray(arr, i) {
  return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest();
}
function _classCallCheck$1(instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
}
function _typeof$2(o) {
  "@babel/helpers - typeof";

  return _typeof$2 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
    return typeof o;
  } : function (o) {
    return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
  }, _typeof$2(o);
}
function toPrimitive$1(t, r) {
  if ("object" != _typeof$2(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r);
    if ("object" != _typeof$2(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(t);
}
function toPropertyKey$1(t) {
  var i = toPrimitive$1(t, "string");
  return "symbol" == _typeof$2(i) ? i : String(i);
}
function _defineProperties$1(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor) descriptor.writable = true;
    Object.defineProperty(target, toPropertyKey$1(descriptor.key), descriptor);
  }
}
function _createClass$1(Constructor, protoProps, staticProps) {
  if (protoProps) _defineProperties$1(Constructor.prototype, protoProps);
  if (staticProps) _defineProperties$1(Constructor, staticProps);
  Object.defineProperty(Constructor, "prototype", {
    writable: false
  });
  return Constructor;
}
function getDefaultExportFromCjs(x) {
  return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, 'default') ? x['default'] : x;
}

/**
 * Types of block
 * @enum {string}
 */
var BlockType = {
  /**
   * Boolean reporter with hexagonal shape
   */
  BOOLEAN: 'Boolean',
  /**
   * A button (not an actual block) for some special action, like making a variable
   */
  BUTTON: 'button',
  /**
   * Command block
   */
  COMMAND: 'command',
  /**
   * Specialized command block which may or may not run a child branch
   * The thread continues with the next block whether or not a child branch ran.
   */
  CONDITIONAL: 'conditional',
  /**
   * Specialized hat block with no implementation function
   * This stack only runs if the corresponding event is emitted by other code.
   */
  EVENT: 'event',
  /**
   * Hat block which conditionally starts a block stack
   */
  HAT: 'hat',
  /**
   * Specialized command block which may or may not run a child branch
   * If a child branch runs, the thread evaluates the loop block again.
   */
  LOOP: 'loop',
  /**
   * General reporter with numeric or string value
   */
  REPORTER: 'reporter'
};
var blockType = BlockType;
var BlockType$1 = /*@__PURE__*/getDefaultExportFromCjs(blockType);

/**
 * Block argument types
 * @enum {string}
 */
var ArgumentType = {
  /**
   * Numeric value with angle picker
   */
  ANGLE: 'angle',
  /**
   * Boolean value with hexagonal placeholder
   */
  BOOLEAN: 'Boolean',
  /**
   * Numeric value with color picker
   */
  COLOR: 'color',
  /**
   * Numeric value with text field
   */
  NUMBER: 'number',
  /**
   * String value with text field
   */
  STRING: 'string',
  /**
   * String value with matrix field
   */
  MATRIX: 'matrix',
  /**
   * MIDI note number with note picker (piano) field
   */
  NOTE: 'note',
  /**
   * Inline image on block (as part of the label)
   */
  IMAGE: 'image'
};
var argumentType = ArgumentType;
var ArgumentType$1 = /*@__PURE__*/getDefaultExportFromCjs(argumentType);
function _classCallCheck(a, n) {
  if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}
function _typeof$1(o) {
  "@babel/helpers - typeof";

  return _typeof$1 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
    return typeof o;
  } : function (o) {
    return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
  }, _typeof$1(o);
}
function toPrimitive(t, r) {
  if ("object" != _typeof$1(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r);
    if ("object" != _typeof$1(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(t);
}
function toPropertyKey(t) {
  var i = toPrimitive(t, "string");
  return "symbol" == _typeof$1(i) ? i : i + "";
}
function _defineProperties(e, r) {
  for (var t = 0; t < r.length; t++) {
    var o = r[t];
    o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e, toPropertyKey(o.key), o);
  }
}
function _createClass(e, r, t) {
  return t && _defineProperties(e, t), Object.defineProperty(e, "prototype", {
    writable: false
  }), e;
}
var Color$1 = /*#__PURE__*/function () {
  function Color() {
    _classCallCheck(this, Color);
  }
  _createClass(Color, null, [{
    key: "RGB_BLACK",
    get:
    /**
     * @typedef {object} RGBObject - An object representing a color in RGB format.
     * @property {number} r - the red component, in the range [0, 255].
     * @property {number} g - the green component, in the range [0, 255].
     * @property {number} b - the blue component, in the range [0, 255].
     */

    /**
     * @typedef {object} HSVObject - An object representing a color in HSV format.
     * @property {number} h - hue, in the range [0-359).
     * @property {number} s - saturation, in the range [0,1].
     * @property {number} v - value, in the range [0,1].
     */

    /** @type {RGBObject} */
    function get() {
      return {
        r: 0,
        g: 0,
        b: 0
      };
    }

    /** @type {RGBObject} */
  }, {
    key: "RGB_WHITE",
    get: function get() {
      return {
        r: 255,
        g: 255,
        b: 255
      };
    }

    /**
     * Convert a Scratch decimal color to a hex string, #RRGGBB.
     * @param {number} decimal RGB color as a decimal.
     * @return {string} RGB color as #RRGGBB hex string.
     */
  }, {
    key: "decimalToHex",
    value: function decimalToHex(decimal) {
      if (decimal < 0) {
        decimal += 0xFFFFFF + 1;
      }
      var hex = Number(decimal).toString(16);
      hex = "#".concat('000000'.substr(0, 6 - hex.length)).concat(hex);
      return hex;
    }

    /**
     * Convert a Scratch decimal color to an RGB color object.
     * @param {number} decimal RGB color as decimal.
     * @return {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     */
  }, {
    key: "decimalToRgb",
    value: function decimalToRgb(decimal) {
      var a = decimal >> 24 & 0xFF;
      var r = decimal >> 16 & 0xFF;
      var g = decimal >> 8 & 0xFF;
      var b = decimal & 0xFF;
      return {
        r: r,
        g: g,
        b: b,
        a: a > 0 ? a : 255
      };
    }

    /**
     * Convert a hex color (e.g., F00, #03F, #0033FF) to an RGB color object.
     * CC-BY-SA Tim Down:
     * https://stackoverflow.com/questions/5623838/rgb-to-hex-and-hex-to-rgb
     * @param {!string} hex Hex representation of the color.
     * @return {RGBObject} null on failure, or rgb: {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     */
  }, {
    key: "hexToRgb",
    value: function hexToRgb(hex) {
      var shorthandRegex = /^#?([a-f\d])([a-f\d])([a-f\d])$/i;
      hex = hex.replace(shorthandRegex, function (m, r, g, b) {
        return r + r + g + g + b + b;
      });
      var result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
      return result ? {
        r: parseInt(result[1], 16),
        g: parseInt(result[2], 16),
        b: parseInt(result[3], 16)
      } : null;
    }

    /**
     * Convert an RGB color object to a hex color.
     * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     * @return {!string} Hex representation of the color.
     */
  }, {
    key: "rgbToHex",
    value: function rgbToHex(rgb) {
      return Color.decimalToHex(Color.rgbToDecimal(rgb));
    }

    /**
     * Convert an RGB color object to a Scratch decimal color.
     * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     * @return {!number} Number representing the color.
     */
  }, {
    key: "rgbToDecimal",
    value: function rgbToDecimal(rgb) {
      return (rgb.r << 16) + (rgb.g << 8) + rgb.b;
    }

    /**
    * Convert a hex color (e.g., F00, #03F, #0033FF) to a decimal color number.
    * @param {!string} hex Hex representation of the color.
    * @return {!number} Number representing the color.
    */
  }, {
    key: "hexToDecimal",
    value: function hexToDecimal(hex) {
      return Color.rgbToDecimal(Color.hexToRgb(hex));
    }

    /**
     * Convert an HSV color to RGB format.
     * @param {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
     * @return {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     */
  }, {
    key: "hsvToRgb",
    value: function hsvToRgb(hsv) {
      var h = hsv.h % 360;
      if (h < 0) h += 360;
      var s = Math.max(0, Math.min(hsv.s, 1));
      var v = Math.max(0, Math.min(hsv.v, 1));
      var i = Math.floor(h / 60);
      var f = h / 60 - i;
      var p = v * (1 - s);
      var q = v * (1 - s * f);
      var t = v * (1 - s * (1 - f));
      var r;
      var g;
      var b;
      switch (i) {
        default:
        case 0:
          r = v;
          g = t;
          b = p;
          break;
        case 1:
          r = q;
          g = v;
          b = p;
          break;
        case 2:
          r = p;
          g = v;
          b = t;
          break;
        case 3:
          r = p;
          g = q;
          b = v;
          break;
        case 4:
          r = t;
          g = p;
          b = v;
          break;
        case 5:
          r = v;
          g = p;
          b = q;
          break;
      }
      return {
        r: Math.floor(r * 255),
        g: Math.floor(g * 255),
        b: Math.floor(b * 255)
      };
    }

    /**
     * Convert an RGB color to HSV format.
     * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     * @return {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
     */
  }, {
    key: "rgbToHsv",
    value: function rgbToHsv(rgb) {
      var r = rgb.r / 255;
      var g = rgb.g / 255;
      var b = rgb.b / 255;
      var x = Math.min(Math.min(r, g), b);
      var v = Math.max(Math.max(r, g), b);

      // For grays, hue will be arbitrarily reported as zero. Otherwise, calculate
      var h = 0;
      var s = 0;
      if (x !== v) {
        var f = r === x ? g - b : g === x ? b - r : r - g;
        var i = r === x ? 3 : g === x ? 5 : 1;
        h = (i - f / (v - x)) * 60 % 360;
        s = (v - x) / v;
      }
      return {
        h: h,
        s: s,
        v: v
      };
    }

    /**
     * Linear interpolation between rgb0 and rgb1.
     * @param {RGBObject} rgb0 - the color corresponding to fraction1 <= 0.
     * @param {RGBObject} rgb1 - the color corresponding to fraction1 >= 1.
     * @param {number} fraction1 - the interpolation parameter. If this is 0.5, for example, mix the two colors equally.
     * @return {RGBObject} the interpolated color.
     */
  }, {
    key: "mixRgb",
    value: function mixRgb(rgb0, rgb1, fraction1) {
      if (fraction1 <= 0) return rgb0;
      if (fraction1 >= 1) return rgb1;
      var fraction0 = 1 - fraction1;
      return {
        r: fraction0 * rgb0.r + fraction1 * rgb1.r,
        g: fraction0 * rgb0.g + fraction1 * rgb1.g,
        b: fraction0 * rgb0.b + fraction1 * rgb1.b
      };
    }
  }]);
  return Color;
}();
var color$3 = Color$1;
var Color = color$3;

/**
 * @fileoverview
 * Utilities for casting and comparing Scratch data-types.
 * Scratch behaves slightly differently from JavaScript in many respects,
 * and these differences should be encapsulated below.
 * For example, in Scratch, add(1, join("hello", world")) -> 1.
 * This is because "hello world" is cast to 0.
 * In JavaScript, 1 + Number("hello" + "world") would give you NaN.
 * Use when coercing a value before computation.
 */
var Cast = /*#__PURE__*/function () {
  function Cast() {
    _classCallCheck(this, Cast);
  }
  _createClass(Cast, null, [{
    key: "toNumber",
    value:
    /**
     * Scratch cast to number.
     * Treats NaN as 0.
     * In Scratch 2.0, this is captured by `interp.numArg.`
     * @param {*} value Value to cast to number.
     * @return {number} The Scratch-casted number value.
     */
    function toNumber(value) {
      // If value is already a number we don't need to coerce it with
      // Number().
      if (typeof value === 'number') {
        // Scratch treats NaN as 0, when needed as a number.
        // E.g., 0 + NaN -> 0.
        if (Number.isNaN(value)) {
          return 0;
        }
        return value;
      }
      if (typeof value === 'string') {
        // Replace full-width numbers with half-width ones.
        value = value.replace(/[０-９＋．ｅ]/g, function (s) {
          return String.fromCharCode(s.charCodeAt(0) - 0xFEE0);
        });
        value = value.replace(/[-－﹣−‐⁃‑‒–—﹘―⎯⏤ーｰ─━]/g, '-');
      }
      var n = Number(value);
      if (Number.isNaN(n)) {
        // Scratch treats NaN as 0, when needed as a number.
        // E.g., 0 + NaN -> 0.
        return 0;
      }
      return n;
    }

    /**
     * Scratch cast to boolean.
     * In Scratch 2.0, this is captured by `interp.boolArg.`
     * Treats some string values differently from JavaScript.
     * @param {*} value Value to cast to boolean.
     * @return {boolean} The Scratch-casted boolean value.
     */
  }, {
    key: "toBoolean",
    value: function toBoolean(value) {
      // Already a boolean?
      if (typeof value === 'boolean') {
        return value;
      }
      if (typeof value === 'string') {
        // These specific strings are treated as false in Scratch.
        if (value === '' || value === '0' || value.toLowerCase() === 'false') {
          return false;
        }
        // All other strings treated as true.
        return true;
      }
      // Coerce other values and numbers.
      return Boolean(value);
    }

    /**
     * Scratch cast to string.
     * @param {*} value Value to cast to string.
     * @return {string} The Scratch-casted string value.
     */
  }, {
    key: "toString",
    value: function toString(value) {
      return String(value).replace(/\\n/g, '\n').replace(/\\t/g, '\t');
    }

    /**
     * Cast any Scratch argument to an RGB color array to be used for the renderer.
     * @param {*} value Value to convert to RGB color array.
     * @return {Array.<number>} [r,g,b], values between 0-255.
     */
  }, {
    key: "toRgbColorList",
    value: function toRgbColorList(value) {
      var color = Cast.toRgbColorObject(value);
      return [color.r, color.g, color.b];
    }

    /**
     * Cast any Scratch argument to an RGB color object to be used for the renderer.
     * @param {*} value Value to convert to RGB color object.
     * @return {RGBOject} [r,g,b], values between 0-255.
     */
  }, {
    key: "toRgbColorObject",
    value: function toRgbColorObject(value) {
      var color;
      if (typeof value === 'string' && value.substring(0, 1) === '#') {
        color = Color.hexToRgb(value);

        // If the color wasn't *actually* a hex color, cast to black
        if (!color) color = {
          r: 0,
          g: 0,
          b: 0,
          a: 255
        };
      } else {
        color = Color.decimalToRgb(Cast.toNumber(value));
      }
      return color;
    }

    /**
     * Determine if a Scratch argument is a white space string (or null / empty).
     * @param {*} val value to check.
     * @return {boolean} True if the argument is all white spaces or null / empty.
     */
  }, {
    key: "isWhiteSpace",
    value: function isWhiteSpace(val) {
      return val === null || typeof val === 'string' && val.trim().length === 0;
    }

    /**
     * Compare two values, using Scratch cast, case-insensitive string compare, etc.
     * In Scratch 2.0, this is captured by `interp.compare.`
     * @param {*} v1 First value to compare.
     * @param {*} v2 Second value to compare.
     * @returns {number} Negative number if v1 < v2; 0 if equal; positive otherwise.
     */
  }, {
    key: "compare",
    value: function compare(v1, v2) {
      var n1 = Number(v1);
      var n2 = Number(v2);
      if (n1 === 0 && Cast.isWhiteSpace(v1)) {
        n1 = NaN;
      } else if (n2 === 0 && Cast.isWhiteSpace(v2)) {
        n2 = NaN;
      }
      if (isNaN(n1) || isNaN(n2)) {
        // At least one argument can't be converted to a number.
        // Scratch compares strings as case insensitive.
        var s1 = Cast.toString(v1).toLowerCase();
        var s2 = Cast.toString(v2).toLowerCase();
        if (s1 < s2) {
          return -1;
        } else if (s1 > s2) {
          return 1;
        }
        return 0;
      }
      // Handle the special case of Infinity
      if (n1 === Infinity && n2 === Infinity || n1 === -Infinity && n2 === -Infinity) {
        return 0;
      }
      // Compare as numbers.
      return n1 - n2;
    }

    /**
     * Determine if a Scratch argument number represents a round integer.
     * @param {*} val Value to check.
     * @return {boolean} True if number looks like an integer.
     */
  }, {
    key: "isInt",
    value: function isInt(val) {
      // Values that are already numbers.
      if (typeof val === 'number') {
        if (isNaN(val)) {
          // NaN is considered an integer.
          return true;
        }
        // True if it's "round" (e.g., 2.0 and 2).
        return val === parseInt(val, 10);
      } else if (typeof val === 'boolean') {
        // `True` and `false` always represent integer after Scratch cast.
        return true;
      } else if (typeof val === 'string') {
        // If it contains a decimal point, don't consider it an int.
        return val.indexOf('.') < 0;
      }
      return false;
    }
  }, {
    key: "LIST_INVALID",
    get: function get() {
      return 'INVALID';
    }
  }, {
    key: "LIST_ALL",
    get: function get() {
      return 'ALL';
    }

    /**
     * Compute a 1-based index into a list, based on a Scratch argument.
     * Two special cases may be returned:
     * LIST_ALL: if the block is referring to all of the items in the list.
     * LIST_INVALID: if the index was invalid in any way.
     * @param {*} index Scratch arg, including 1-based numbers or special cases.
     * @param {number} length Length of the list.
     * @param {boolean} acceptAll Whether it should accept "all" or not.
     * @return {(number|string)} 1-based index for list, LIST_ALL, or LIST_INVALID.
     */
  }, {
    key: "toListIndex",
    value: function toListIndex(index, length, acceptAll) {
      if (typeof index !== 'number') {
        if (index === 'all') {
          return acceptAll ? Cast.LIST_ALL : Cast.LIST_INVALID;
        }
        if (index === 'last') {
          if (length > 0) {
            return length;
          }
          return Cast.LIST_INVALID;
        } else if (index === 'random' || index === 'any') {
          if (length > 0) {
            return 1 + Math.floor(Math.random() * length);
          }
          return Cast.LIST_INVALID;
        }
      }
      index = Math.floor(Cast.toNumber(index));
      if (index < 1 || index > length) {
        return Cast.LIST_INVALID;
      }
      return index;
    }
  }]);
  return Cast;
}();
var cast = Cast;
var Cast$1 = /*@__PURE__*/getDefaultExportFromCjs(cast);
var web = {
  exports: {}
};
var minilog$2 = {
  exports: {}
};
function M() {
  this._events = {};
}
M.prototype = {
  on: function on(ev, cb) {
    this._events || (this._events = {});
    var e = this._events;
    (e[ev] || (e[ev] = [])).push(cb);
    return this;
  },
  removeListener: function removeListener(ev, cb) {
    var e = this._events[ev] || [],
      i;
    for (i = e.length - 1; i >= 0 && e[i]; i--) {
      if (e[i] === cb || e[i].cb === cb) {
        e.splice(i, 1);
      }
    }
  },
  removeAllListeners: function removeAllListeners(ev) {
    if (!ev) {
      this._events = {};
    } else {
      this._events[ev] && (this._events[ev] = []);
    }
  },
  listeners: function listeners(ev) {
    return this._events ? this._events[ev] || [] : [];
  },
  emit: function emit(ev) {
    this._events || (this._events = {});
    var args = Array.prototype.slice.call(arguments, 1),
      i,
      e = this._events[ev] || [];
    for (i = e.length - 1; i >= 0 && e[i]; i--) {
      e[i].apply(this, args);
    }
    return this;
  },
  when: function when(ev, cb) {
    return this.once(ev, cb, true);
  },
  once: function once(ev, cb, when) {
    if (!cb) return this;
    function c() {
      if (!when) this.removeListener(ev, c);
      if (cb.apply(this, arguments) && when) this.removeListener(ev, c);
    }
    c.cb = cb;
    this.on(ev, c);
    return this;
  }
};
M.mixin = function (dest) {
  var o = M.prototype,
    k;
  for (k in o) {
    o.hasOwnProperty(k) && (dest.prototype[k] = o[k]);
  }
};
var microee$1 = M;
var microee = microee$1;

// Implements a subset of Node's stream.Transform - in a cross-platform manner.
function Transform$4() {}
microee.mixin(Transform$4);

// The write() signature is different from Node's
// --> makes it much easier to work with objects in logs.
// One of the lessons from v1 was that it's better to target
// a good browser rather than the lowest common denominator
// internally.
// If you want to use external streams, pipe() to ./stringify.js first.
Transform$4.prototype.write = function (name, level, args) {
  this.emit('item', name, level, args);
};
Transform$4.prototype.end = function () {
  this.emit('end');
  this.removeAllListeners();
};
Transform$4.prototype.pipe = function (dest) {
  var s = this;
  // prevent double piping
  s.emit('unpipe', dest);
  // tell the dest that it's being piped to
  dest.emit('pipe', s);
  function onItem() {
    dest.write.apply(dest, Array.prototype.slice.call(arguments));
  }
  function onEnd() {
    !dest._isStdio && dest.end();
  }
  s.on('item', onItem);
  s.on('end', onEnd);
  s.when('unpipe', function (from) {
    var match = from === dest || typeof from == 'undefined';
    if (match) {
      s.removeListener('item', onItem);
      s.removeListener('end', onEnd);
      dest.emit('unpipe');
    }
    return match;
  });
  return dest;
};
Transform$4.prototype.unpipe = function (from) {
  this.emit('unpipe', from);
  return this;
};
Transform$4.prototype.format = function (dest) {
  throw new Error(['Warning: .format() is deprecated in Minilog v2! Use .pipe() instead. For example:', 'var Minilog = require(\'minilog\');', 'Minilog', '  .pipe(Minilog.backends.console.formatClean)', '  .pipe(Minilog.backends.console);'].join('\n'));
};
Transform$4.mixin = function (dest) {
  var o = Transform$4.prototype,
    k;
  for (k in o) {
    o.hasOwnProperty(k) && (dest.prototype[k] = o[k]);
  }
};
var transform = Transform$4;

// default filter
var Transform$3 = transform;
var levelMap = {
  debug: 1,
  info: 2,
  warn: 3,
  error: 4
};
function Filter() {
  this.enabled = true;
  this.defaultResult = true;
  this.clear();
}
Transform$3.mixin(Filter);

// allow all matching, with level >= given level
Filter.prototype.allow = function (name, level) {
  this._white.push({
    n: name,
    l: levelMap[level]
  });
  return this;
};

// deny all matching, with level <= given level
Filter.prototype.deny = function (name, level) {
  this._black.push({
    n: name,
    l: levelMap[level]
  });
  return this;
};
Filter.prototype.clear = function () {
  this._white = [];
  this._black = [];
  return this;
};
function test(rule, name) {
  // use .test for RegExps
  return rule.n.test ? rule.n.test(name) : rule.n == name;
}
Filter.prototype.test = function (name, level) {
  var i,
    len = Math.max(this._white.length, this._black.length);
  for (i = 0; i < len; i++) {
    if (this._white[i] && test(this._white[i], name) && levelMap[level] >= this._white[i].l) {
      return true;
    }
    if (this._black[i] && test(this._black[i], name) && levelMap[level] <= this._black[i].l) {
      return false;
    }
  }
  return this.defaultResult;
};
Filter.prototype.write = function (name, level, args) {
  if (!this.enabled || this.test(name, level)) {
    return this.emit('item', name, level, args);
  }
};
var filter = Filter;
(function (module, exports) {
  var Transform = transform,
    Filter = filter;
  var log = new Transform(),
    slice = Array.prototype.slice;
  exports = module.exports = function create(name) {
    var o = function o() {
      log.write(name, undefined, slice.call(arguments));
      return o;
    };
    o.debug = function () {
      log.write(name, 'debug', slice.call(arguments));
      return o;
    };
    o.info = function () {
      log.write(name, 'info', slice.call(arguments));
      return o;
    };
    o.warn = function () {
      log.write(name, 'warn', slice.call(arguments));
      return o;
    };
    o.error = function () {
      log.write(name, 'error', slice.call(arguments));
      return o;
    };
    o.log = o.debug; // for interface compliance with Node and browser consoles
    o.suggest = exports.suggest;
    o.format = log.format;
    return o;
  };

  // filled in separately
  exports.defaultBackend = exports.defaultFormatter = null;
  exports.pipe = function (dest) {
    return log.pipe(dest);
  };
  exports.end = exports.unpipe = exports.disable = function (from) {
    return log.unpipe(from);
  };
  exports.Transform = Transform;
  exports.Filter = Filter;
  // this is the default filter that's applied when .enable() is called normally
  // you can bypass it completely and set up your own pipes
  exports.suggest = new Filter();
  exports.enable = function () {
    if (exports.defaultFormatter) {
      return log.pipe(exports.suggest) // filter
      .pipe(exports.defaultFormatter) // formatter
      .pipe(exports.defaultBackend); // backend
    }
    return log.pipe(exports.suggest) // filter
    .pipe(exports.defaultBackend); // formatter
  };
})(minilog$2, minilog$2.exports);
var minilogExports = minilog$2.exports;
var hex = {
  black: '#000',
  red: '#c23621',
  green: '#25bc26',
  yellow: '#bbbb00',
  blue: '#492ee1',
  magenta: '#d338d3',
  cyan: '#33bbc8',
  gray: '#808080',
  purple: '#708'
};
function color$2(fg, isInverse) {
  if (isInverse) {
    return 'color: #fff; background: ' + hex[fg] + ';';
  } else {
    return 'color: ' + hex[fg] + ';';
  }
}
var util = color$2;
var Transform$2 = transform,
  color$1 = util;
var colors$1 = {
    debug: ['cyan'],
    info: ['purple'],
    warn: ['yellow', true],
    error: ['red', true]
  },
  logger$2 = new Transform$2();
logger$2.write = function (name, level, args) {
  var fn = console.log;
  if (console[level] && console[level].apply) {
    fn = console[level];
    fn.apply(console, ['%c' + name + ' %c' + level, color$1('gray'), color$1.apply(color$1, colors$1[level])].concat(args));
  }
};

// NOP, because piping the formatted logs can only cause trouble.
logger$2.pipe = function () {};
var color_1 = logger$2;
var Transform$1 = transform,
  color = util,
  colors = {
    debug: ['gray'],
    info: ['purple'],
    warn: ['yellow', true],
    error: ['red', true]
  },
  logger$1 = new Transform$1();
logger$1.write = function (name, level, args) {
  var fn = console.log;
  if (level != 'debug' && console[level]) {
    fn = console[level];
  }
  var i = 0;
  if (level != 'info') {
    for (; i < args.length; i++) {
      if (typeof args[i] != 'string') break;
    }
    fn.apply(console, ['%c' + name + ' ' + args.slice(0, i).join(' '), color.apply(color, colors[level])].concat(args.slice(i)));
  } else {
    fn.apply(console, ['%c' + name, color.apply(color, colors[level])].concat(args));
  }
};

// NOP, because piping the formatted logs can only cause trouble.
logger$1.pipe = function () {};
var minilog$1 = logger$1;
var Transform = transform;
var newlines = /\n+$/,
  logger = new Transform();
logger.write = function (name, level, args) {
  var i = args.length - 1;
  if (typeof console === 'undefined' || !console.log) {
    return;
  }
  if (console.log.apply) {
    return console.log.apply(console, [name, level].concat(args));
  } else if (JSON && JSON.stringify) {
    // console.log.apply is undefined in IE8 and IE9
    // for IE8/9: make console.log at least a bit less awful
    if (args[i] && typeof args[i] == 'string') {
      args[i] = args[i].replace(newlines, '');
    }
    try {
      for (i = 0; i < args.length; i++) {
        args[i] = JSON.stringify(args[i]);
      }
    } catch (e) {}
    console.log(args.join(' '));
  }
};
logger.formatters = ['color', 'minilog'];
logger.color = color_1;
logger.minilog = minilog$1;
var console_1 = logger;
var array;
var hasRequiredArray;
function requireArray() {
  if (hasRequiredArray) return array;
  hasRequiredArray = 1;
  var Transform = transform,
    cache = [];
  var logger = new Transform();
  logger.write = function (name, level, args) {
    cache.push([name, level, args]);
  };

  // utility functions
  logger.get = function () {
    return cache;
  };
  logger.empty = function () {
    cache = [];
  };
  array = logger;
  return array;
}
var localstorage;
var hasRequiredLocalstorage;
function requireLocalstorage() {
  if (hasRequiredLocalstorage) return localstorage;
  hasRequiredLocalstorage = 1;
  var Transform = transform,
    cache = false;
  var logger = new Transform();
  logger.write = function (name, level, args) {
    if (typeof window == 'undefined' || typeof JSON == 'undefined' || !JSON.stringify || !JSON.parse) return;
    try {
      if (!cache) {
        cache = window.localStorage.minilog ? JSON.parse(window.localStorage.minilog) : [];
      }
      cache.push([new Date().toString(), name, level, args]);
      window.localStorage.minilog = JSON.stringify(cache);
    } catch (e) {}
  };
  localstorage = logger;
  return localstorage;
}
var jquery_simple;
var hasRequiredJquery_simple;
function requireJquery_simple() {
  if (hasRequiredJquery_simple) return jquery_simple;
  hasRequiredJquery_simple = 1;
  var Transform = transform;
  var cid = new Date().valueOf().toString(36);
  function AjaxLogger(options) {
    this.url = options.url || '';
    this.cache = [];
    this.timer = null;
    this.interval = options.interval || 30 * 1000;
    this.enabled = true;
    this.jQuery = window.jQuery;
    this.extras = {};
  }
  Transform.mixin(AjaxLogger);
  AjaxLogger.prototype.write = function (name, level, args) {
    if (!this.timer) {
      this.init();
    }
    this.cache.push([name, level].concat(args));
  };
  AjaxLogger.prototype.init = function () {
    if (!this.enabled || !this.jQuery) return;
    var self = this;
    this.timer = setTimeout(function () {
      var i,
        logs = [],
        ajaxData,
        url = self.url;
      if (self.cache.length == 0) return self.init();
      // Test each log line and only log the ones that are valid (e.g. don't have circular references).
      // Slight performance hit but benefit is we log all valid lines.
      for (i = 0; i < self.cache.length; i++) {
        try {
          JSON.stringify(self.cache[i]);
          logs.push(self.cache[i]);
        } catch (e) {}
      }
      if (self.jQuery.isEmptyObject(self.extras)) {
        ajaxData = JSON.stringify({
          logs: logs
        });
        url = self.url + '?client_id=' + cid;
      } else {
        ajaxData = JSON.stringify(self.jQuery.extend({
          logs: logs
        }, self.extras));
      }
      self.jQuery.ajax(url, {
        type: 'POST',
        cache: false,
        processData: false,
        data: ajaxData,
        contentType: 'application/json',
        timeout: 10000
      }).success(function (data, status, jqxhr) {
        if (data.interval) {
          self.interval = Math.max(1000, data.interval);
        }
      }).error(function () {
        self.interval = 30000;
      }).always(function () {
        self.init();
      });
      self.cache = [];
    }, this.interval);
  };
  AjaxLogger.prototype.end = function () {};

  // wait until jQuery is defined. Useful if you don't control the load order.
  AjaxLogger.jQueryWait = function (onDone) {
    if (typeof window !== 'undefined' && (window.jQuery || window.$)) {
      return onDone(window.jQuery || window.$);
    } else if (typeof window !== 'undefined') {
      setTimeout(function () {
        AjaxLogger.jQueryWait(onDone);
      }, 200);
    }
  };
  jquery_simple = AjaxLogger;
  return jquery_simple;
}
(function (module, exports) {
  var Minilog = minilogExports;
  var oldEnable = Minilog.enable,
    oldDisable = Minilog.disable,
    isChrome = typeof navigator != 'undefined' && /chrome/i.test(navigator.userAgent),
    console = console_1;

  // Use a more capable logging backend if on Chrome
  Minilog.defaultBackend = isChrome ? console.minilog : console;

  // apply enable inputs from localStorage and from the URL
  if (typeof window != 'undefined') {
    try {
      Minilog.enable(JSON.parse(window.localStorage['minilogSettings']));
    } catch (e) {}
    if (window.location && window.location.search) {
      var match = RegExp('[?&]minilog=([^&]*)').exec(window.location.search);
      match && Minilog.enable(decodeURIComponent(match[1]));
    }
  }

  // Make enable also add to localStorage
  Minilog.enable = function () {
    oldEnable.call(Minilog, true);
    try {
      window.localStorage['minilogSettings'] = JSON.stringify(true);
    } catch (e) {}
    return this;
  };
  Minilog.disable = function () {
    oldDisable.call(Minilog);
    try {
      delete window.localStorage.minilogSettings;
    } catch (e) {}
    return this;
  };
  exports = module.exports = Minilog;
  exports.backends = {
    array: requireArray(),
    browser: Minilog.defaultBackend,
    localStorage: requireLocalstorage(),
    jQuery: requireJquery_simple()
  };
})(web, web.exports);
var webExports = web.exports;
var minilog = webExports;
minilog.enable();
var log = minilog('vm');
var log$1 = /*@__PURE__*/getDefaultExportFromCjs(log);
var en = {
  "costumex.name": "CostumeX",
  "costumex.takeSnapshot": "snapshot center x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]",
  "costumex.insertSnapshotAsCostume": "insert snapshot [NAME] at [INDEX] x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]",
  "costumex.insertSnapshotAsCostume.defaultCostumeName": "snapshot",
  "costumex.captureVideoFrame": "capture video center x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]",
  "costumex.insertCameraImageAsCostume": "insert camera image [NAME] at [INDEX] x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]",
  "costumex.insertCameraImageAsCostume.defaultCostumeName": "camera",
  "costumex.insertImageAsCostume": "insert costume [NAME] at [INDEX] width [WIDTH] height [HEIGHT] with image [DATA]",
  "costumex.insertImageAsCostume.defaultCostumeName": "name",
  "costumex.deleteCostume": "delete costume [COSTUME]",
  "costumex.costumesLength": "costumes length",
  "costumex.costumeNameAt": "costume name at [INDEX]",
  "costumex.costumeData": "image data of costume [COSTUME]",
  "costumex.costumeSize": "[DIMENSION] of costume [COSTUME]",
  "costumex.dimensionMenu.width": "width",
  "costumex.dimensionMenu.height": "height",
  "costumex.dimensionMenu.centerX": "center x",
  "costumex.dimensionMenu.centerY": "center y",
  "costumex.flipCostume": "flip costume [COSTUME] [DIRECTION]",
  "costumex.flipDirectionMenu.horizontal": "left-right",
  "costumex.flipDirectionMenu.vertical": "up-down"
};
var ja = {
  "costumex.name": "CostumeX",
  "costumex.takeSnapshot": "スナップショット 中心 x:[X] y:[Y] 幅:[WIDTH] 高さ:[HEIGHT]",
  "costumex.insertSnapshotAsCostume": "コスチューム名[NAME]として[INDEX]番目にスナップショット x:[X] y:[Y] 幅:[WIDTH] 高さ:[HEIGHT]を挿入する",
  "costumex.insertSnapshotAsCostume.defaultCostumeName": "スナップショット",
  "costumex.captureVideoFrame": "ビデオキャプチャ 中心 x:[X] y:[Y] 幅:[WIDTH] 高さ:[HEIGHT]",
  "costumex.insertCameraImageAsCostume": "コスチューム名[NAME]として[INDEX]番目にカメラ画像 x:[X] y:[Y] 幅:[WIDTH] 高さ:[HEIGHT]を挿入する",
  "costumex.insertCameraImageAsCostume.defaultCostumeName": "カメラ",
  "costumex.insertImageAsCostume": "コスチューム名[NAME]として[INDEX]番目に幅[WIDTH]高さ[HEIGHT]の画像[DATA]を挿入する",
  "costumex.insertImageAsCostume.defaultCostumeName": "名前",
  "costumex.deleteCostume": "コスチューム[COSTUME]を削除する",
  "costumex.costumesLength": "コスチュームの数",
  "costumex.costumeNameAt": "[INDEX]番目のコスチュームの名前",
  "costumex.costumeData": "コスチューム[COSTUME]のデータ",
  "costumex.costumeSize": "コスチューム[COSTUME]の[DIMENSION]",
  "costumex.dimensionMenu.width": "幅",
  "costumex.dimensionMenu.height": "高さ",
  "costumex.dimensionMenu.centerX": "中心のX",
  "costumex.dimensionMenu.centerY": "中心のY",
  "costumex.flipCostume": "コスチューム[COSTUME]を[DIRECTION]反転する",
  "costumex.flipDirectionMenu.horizontal": "左右",
  "costumex.flipDirectionMenu.vertical": "上下"
};
var translations = {
  en: en,
  ja: ja,
  "ja-Hira": {
    "costumex.name": "CostumeX",
    "costumex.takeSnapshot": "スナップショット ちゅうしん x:[X] y:[Y] はば:[WIDTH] たかさ:[HEIGHT]",
    "costumex.insertSnapshotAsCostume": "コスチュームめい[NAME]として[INDEX]ばんめ に スナップショット x:[X] y:[Y] はば:[WIDTH] たかさ:[HEIGHT]を そうにゅう する",
    "costumex.insertSnapshotAsCostume.defaultCostumeName": "スナップショット",
    "costumex.captureVideoFrame": "ビデオキャプチャ ちゅうしん x:[X] y:[Y] はば:[WIDTH] たかさ:[HEIGHT]",
    "costumex.insertCameraImageAsCostume": "コスチュームめい[NAME]として[INDEX]ばんめ に カメラがぞう x:[X] y:[Y] はば:[WIDTH] たかさ:[HEIGHT]を そうにゅう する",
    "costumex.insertCameraImageAsCostume.defaultCostumeName": "カメラ",
    "costumex.insertImageAsCostume": "コスチュームめい[NAME]として[INDEX]ばんめ に はば[WIDTH]たかさ[HEIGHT]の がぞう[DATA]を そうにゅう する",
    "costumex.insertImageAsCostume.defaultCostumeName": "なまえ",
    "costumex.deleteCostume": "コスチューム[COSTUME]を さくじょ する",
    "costumex.costumesLength": "コスチューム の かず",
    "costumex.costumeNameAt": "[INDEX]ばんめ の コスチューム の なまえ",
    "costumex.costumeData": "コスチューム[COSTUME] の データ",
    "costumex.costumeSize": "コスチューム[COSTUME] の [DIMENSION]",
    "costumex.dimensionMenu.width": "はば",
    "costumex.dimensionMenu.height": "たかさ",
    "costumex.dimensionMenu.centerX": "ちゅうしん の X",
    "costumex.dimensionMenu.centerY": "ちゅうしん の Y",
    "costumex.flipCostume": "コスチューム[COSTUME]を[DIRECTION]はんてん する",
    "costumex.flipDirectionMenu.horizontal": "さゆう",
    "costumex.flipDirectionMenu.vertical": "じょうげ"
  }
};
var img = "data:image/png;base64,";

/**
 * Whether debug mode is enabled.
 * @type {boolean}
 */
var DEBUG;

/**
 * Check if debug mode is enabled.
 * @returns {boolean} - true if debug mode is enabled.
 */
var checkDebugMode = function checkDebugMode() {
  if (typeof DEBUG !== 'undefined') {
    return DEBUG;
  }
  try {
    var urlParams = new URLSearchParams(window.location.href.split('?')[1].toLowerCase());
    if (urlParams.get('debug') === 'true' || urlParams.get('debug') === '') {
      DEBUG = true;
    } else {
      DEBUG = false;
    }
  } catch (error) {
    // ignore
  }
  return DEBUG;
};
function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) {
  try {
    var info = gen[key](arg);
    var value = info.value;
  } catch (error) {
    reject(error);
    return;
  }
  if (info.done) {
    resolve(value);
  } else {
    Promise.resolve(value).then(_next, _throw);
  }
}
function _asyncToGenerator(fn) {
  return function () {
    var self = this,
      args = arguments;
    return new Promise(function (resolve, reject) {
      var gen = fn.apply(self, args);
      function _next(value) {
        asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value);
      }
      function _throw(err) {
        asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err);
      }
      _next(undefined);
    });
  };
}
var regeneratorRuntime$1 = {
  exports: {}
};
var _typeof = {
  exports: {}
};
(function (module) {
  function _typeof(o) {
    "@babel/helpers - typeof";

    return module.exports = _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
      return typeof o;
    } : function (o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, module.exports.__esModule = true, module.exports["default"] = module.exports, _typeof(o);
  }
  module.exports = _typeof, module.exports.__esModule = true, module.exports["default"] = module.exports;
})(_typeof);
var _typeofExports = _typeof.exports;
(function (module) {
  var _typeof = _typeofExports["default"];
  function _regeneratorRuntime() {
    module.exports = _regeneratorRuntime = function _regeneratorRuntime() {
      return e;
    }, module.exports.__esModule = true, module.exports["default"] = module.exports;
    var t,
      e = {},
      r = Object.prototype,
      n = r.hasOwnProperty,
      o = Object.defineProperty || function (t, e, r) {
        t[e] = r.value;
      },
      i = "function" == typeof Symbol ? Symbol : {},
      a = i.iterator || "@@iterator",
      c = i.asyncIterator || "@@asyncIterator",
      u = i.toStringTag || "@@toStringTag";
    function define(t, e, r) {
      return Object.defineProperty(t, e, {
        value: r,
        enumerable: true,
        configurable: true,
        writable: true
      }), t[e];
    }
    try {
      define({}, "");
    } catch (t) {
      define = function define(t, e, r) {
        return t[e] = r;
      };
    }
    function wrap(t, e, r, n) {
      var i = e && e.prototype instanceof Generator ? e : Generator,
        a = Object.create(i.prototype),
        c = new Context(n || []);
      return o(a, "_invoke", {
        value: makeInvokeMethod(t, r, c)
      }), a;
    }
    function tryCatch(t, e, r) {
      try {
        return {
          type: "normal",
          arg: t.call(e, r)
        };
      } catch (t) {
        return {
          type: "throw",
          arg: t
        };
      }
    }
    e.wrap = wrap;
    var h = "suspendedStart",
      l = "suspendedYield",
      f = "executing",
      s = "completed",
      y = {};
    function Generator() {}
    function GeneratorFunction() {}
    function GeneratorFunctionPrototype() {}
    var p = {};
    define(p, a, function () {
      return this;
    });
    var d = Object.getPrototypeOf,
      v = d && d(d(values([])));
    v && v !== r && n.call(v, a) && (p = v);
    var g = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(p);
    function defineIteratorMethods(t) {
      ["next", "throw", "return"].forEach(function (e) {
        define(t, e, function (t) {
          return this._invoke(e, t);
        });
      });
    }
    function AsyncIterator(t, e) {
      function invoke(r, o, i, a) {
        var c = tryCatch(t[r], t, o);
        if ("throw" !== c.type) {
          var u = c.arg,
            h = u.value;
          return h && "object" == _typeof(h) && n.call(h, "__await") ? e.resolve(h.__await).then(function (t) {
            invoke("next", t, i, a);
          }, function (t) {
            invoke("throw", t, i, a);
          }) : e.resolve(h).then(function (t) {
            u.value = t, i(u);
          }, function (t) {
            return invoke("throw", t, i, a);
          });
        }
        a(c.arg);
      }
      var r;
      o(this, "_invoke", {
        value: function value(t, n) {
          function callInvokeWithMethodAndArg() {
            return new e(function (e, r) {
              invoke(t, n, e, r);
            });
          }
          return r = r ? r.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg();
        }
      });
    }
    function makeInvokeMethod(e, r, n) {
      var o = h;
      return function (i, a) {
        if (o === f) throw new Error("Generator is already running");
        if (o === s) {
          if ("throw" === i) throw a;
          return {
            value: t,
            done: true
          };
        }
        for (n.method = i, n.arg = a;;) {
          var c = n.delegate;
          if (c) {
            var u = maybeInvokeDelegate(c, n);
            if (u) {
              if (u === y) continue;
              return u;
            }
          }
          if ("next" === n.method) n.sent = n._sent = n.arg;else if ("throw" === n.method) {
            if (o === h) throw o = s, n.arg;
            n.dispatchException(n.arg);
          } else "return" === n.method && n.abrupt("return", n.arg);
          o = f;
          var p = tryCatch(e, r, n);
          if ("normal" === p.type) {
            if (o = n.done ? s : l, p.arg === y) continue;
            return {
              value: p.arg,
              done: n.done
            };
          }
          "throw" === p.type && (o = s, n.method = "throw", n.arg = p.arg);
        }
      };
    }
    function maybeInvokeDelegate(e, r) {
      var n = r.method,
        o = e.iterator[n];
      if (o === t) return r.delegate = null, "throw" === n && e.iterator["return"] && (r.method = "return", r.arg = t, maybeInvokeDelegate(e, r), "throw" === r.method) || "return" !== n && (r.method = "throw", r.arg = new TypeError("The iterator does not provide a '" + n + "' method")), y;
      var i = tryCatch(o, e.iterator, r.arg);
      if ("throw" === i.type) return r.method = "throw", r.arg = i.arg, r.delegate = null, y;
      var a = i.arg;
      return a ? a.done ? (r[e.resultName] = a.value, r.next = e.nextLoc, "return" !== r.method && (r.method = "next", r.arg = t), r.delegate = null, y) : a : (r.method = "throw", r.arg = new TypeError("iterator result is not an object"), r.delegate = null, y);
    }
    function pushTryEntry(t) {
      var e = {
        tryLoc: t[0]
      };
      1 in t && (e.catchLoc = t[1]), 2 in t && (e.finallyLoc = t[2], e.afterLoc = t[3]), this.tryEntries.push(e);
    }
    function resetTryEntry(t) {
      var e = t.completion || {};
      e.type = "normal", delete e.arg, t.completion = e;
    }
    function Context(t) {
      this.tryEntries = [{
        tryLoc: "root"
      }], t.forEach(pushTryEntry, this), this.reset(true);
    }
    function values(e) {
      if (e || "" === e) {
        var r = e[a];
        if (r) return r.call(e);
        if ("function" == typeof e.next) return e;
        if (!isNaN(e.length)) {
          var o = -1,
            i = function next() {
              for (; ++o < e.length;) if (n.call(e, o)) return next.value = e[o], next.done = false, next;
              return next.value = t, next.done = true, next;
            };
          return i.next = i;
        }
      }
      throw new TypeError(_typeof(e) + " is not iterable");
    }
    return GeneratorFunction.prototype = GeneratorFunctionPrototype, o(g, "constructor", {
      value: GeneratorFunctionPrototype,
      configurable: true
    }), o(GeneratorFunctionPrototype, "constructor", {
      value: GeneratorFunction,
      configurable: true
    }), GeneratorFunction.displayName = define(GeneratorFunctionPrototype, u, "GeneratorFunction"), e.isGeneratorFunction = function (t) {
      var e = "function" == typeof t && t.constructor;
      return !!e && (e === GeneratorFunction || "GeneratorFunction" === (e.displayName || e.name));
    }, e.mark = function (t) {
      return Object.setPrototypeOf ? Object.setPrototypeOf(t, GeneratorFunctionPrototype) : (t.__proto__ = GeneratorFunctionPrototype, define(t, u, "GeneratorFunction")), t.prototype = Object.create(g), t;
    }, e.awrap = function (t) {
      return {
        __await: t
      };
    }, defineIteratorMethods(AsyncIterator.prototype), define(AsyncIterator.prototype, c, function () {
      return this;
    }), e.AsyncIterator = AsyncIterator, e.async = function (t, r, n, o, i) {
      void 0 === i && (i = Promise);
      var a = new AsyncIterator(wrap(t, r, n, o), i);
      return e.isGeneratorFunction(r) ? a : a.next().then(function (t) {
        return t.done ? t.value : a.next();
      });
    }, defineIteratorMethods(g), define(g, u, "Generator"), define(g, a, function () {
      return this;
    }), define(g, "toString", function () {
      return "[object Generator]";
    }), e.keys = function (t) {
      var e = Object(t),
        r = [];
      for (var n in e) r.push(n);
      return r.reverse(), function next() {
        for (; r.length;) {
          var t = r.pop();
          if (t in e) return next.value = t, next.done = false, next;
        }
        return next.done = true, next;
      };
    }, e.values = values, Context.prototype = {
      constructor: Context,
      reset: function reset(e) {
        if (this.prev = 0, this.next = 0, this.sent = this._sent = t, this.done = false, this.delegate = null, this.method = "next", this.arg = t, this.tryEntries.forEach(resetTryEntry), !e) for (var r in this) "t" === r.charAt(0) && n.call(this, r) && !isNaN(+r.slice(1)) && (this[r] = t);
      },
      stop: function stop() {
        this.done = true;
        var t = this.tryEntries[0].completion;
        if ("throw" === t.type) throw t.arg;
        return this.rval;
      },
      dispatchException: function dispatchException(e) {
        if (this.done) throw e;
        var r = this;
        function handle(n, o) {
          return a.type = "throw", a.arg = e, r.next = n, o && (r.method = "next", r.arg = t), !!o;
        }
        for (var o = this.tryEntries.length - 1; o >= 0; --o) {
          var i = this.tryEntries[o],
            a = i.completion;
          if ("root" === i.tryLoc) return handle("end");
          if (i.tryLoc <= this.prev) {
            var c = n.call(i, "catchLoc"),
              u = n.call(i, "finallyLoc");
            if (c && u) {
              if (this.prev < i.catchLoc) return handle(i.catchLoc, true);
              if (this.prev < i.finallyLoc) return handle(i.finallyLoc);
            } else if (c) {
              if (this.prev < i.catchLoc) return handle(i.catchLoc, true);
            } else {
              if (!u) throw new Error("try statement without catch or finally");
              if (this.prev < i.finallyLoc) return handle(i.finallyLoc);
            }
          }
        }
      },
      abrupt: function abrupt(t, e) {
        for (var r = this.tryEntries.length - 1; r >= 0; --r) {
          var o = this.tryEntries[r];
          if (o.tryLoc <= this.prev && n.call(o, "finallyLoc") && this.prev < o.finallyLoc) {
            var i = o;
            break;
          }
        }
        i && ("break" === t || "continue" === t) && i.tryLoc <= e && e <= i.finallyLoc && (i = null);
        var a = i ? i.completion : {};
        return a.type = t, a.arg = e, i ? (this.method = "next", this.next = i.finallyLoc, y) : this.complete(a);
      },
      complete: function complete(t, e) {
        if ("throw" === t.type) throw t.arg;
        return "break" === t.type || "continue" === t.type ? this.next = t.arg : "return" === t.type ? (this.rval = this.arg = t.arg, this.method = "return", this.next = "end") : "normal" === t.type && e && (this.next = e), y;
      },
      finish: function finish(t) {
        for (var e = this.tryEntries.length - 1; e >= 0; --e) {
          var r = this.tryEntries[e];
          if (r.finallyLoc === t) return this.complete(r.completion, r.afterLoc), resetTryEntry(r), y;
        }
      },
      "catch": function _catch(t) {
        for (var e = this.tryEntries.length - 1; e >= 0; --e) {
          var r = this.tryEntries[e];
          if (r.tryLoc === t) {
            var n = r.completion;
            if ("throw" === n.type) {
              var o = n.arg;
              resetTryEntry(r);
            }
            return o;
          }
        }
        throw new Error("illegal catch attempt");
      },
      delegateYield: function delegateYield(e, r, n) {
        return this.delegate = {
          iterator: values(e),
          resultName: r,
          nextLoc: n
        }, "next" === this.method && (this.arg = t), y;
      }
    }, e;
  }
  module.exports = _regeneratorRuntime, module.exports.__esModule = true, module.exports["default"] = module.exports;
})(regeneratorRuntime$1);
var regeneratorRuntimeExports = regeneratorRuntime$1.exports;

// TODO(Babel 8): Remove this file.

var runtime = regeneratorRuntimeExports();
var regenerator = runtime;

// Copied from https://github.com/facebook/regenerator/blob/main/packages/runtime/runtime.js#L736=
try {
  regeneratorRuntime = runtime;
} catch (accidentalStrictMode) {
  if (typeof globalThis === "object") {
    globalThis.regeneratorRuntime = runtime;
  } else {
    Function("r", "regeneratorRuntime = r")(runtime);
  }
}
var _regeneratorRuntime = /*@__PURE__*/getDefaultExportFromCjs(regenerator);

/**
 * Convert full-width characters to half-width characters.
 * @param {string} str - string to convert
 * @returns {string} - converted string
 */
var convertToHalfWidthInt = function convertToHalfWidthInt(str) {
  return str.replace(/[０-９]/g, function (s) {
    return String.fromCharCode(s.charCodeAt(0) - 0xFEE0);
  }).replace(/[-－﹣−‐⁃‑‒–—﹘―⎯⏤ーｰ─━]/g, '-');
};

/**
 * Convert array index from one-base to zero-base.
 * If index is negative, it is converted from the end of the array.
 * Returns most close index if index is out of range.
 * @param {number} index - one-base array index
 * @param {number} length - array length
 * @returns {number | undefined} - converted array index
 */
var convertToZeroBaseIndex = function convertToZeroBaseIndex(index, length) {
  if (length === 0) {
    return;
  }
  if (index > length) {
    return length - 1;
  }
  if (index < 0) {
    index = length + index;
    if (index < 0) {
      return 0;
    }
  } else {
    index--;
  }
  return index;
};

/**
 * Get costume index by name or number.
 * If costumeName was not found and it is a number, it is treated as a one-base index.
 * Then that index was a negative number, it is treated as an index from the end of the costume list.
 * @param {Target!} target - target to get costume
 * @param {string} costumeName - name or number of the costume
 * @returns {number?} - costume index
 */
var getCostumeIndexByNameOrNumber = function getCostumeIndexByNameOrNumber(target, costumeName) {
  var costumeArray = target.getCostumes();
  var costumeIndex = costumeArray.findIndex(function (c) {
    return c.name === costumeName;
  });
  if (costumeIndex === -1) {
    var costumeNumber = parseInt(convertToHalfWidthInt(costumeName), 10);
    if (isNaN(costumeNumber) || costumeNumber === 0) {
      return null;
    }
    costumeIndex = convertToZeroBaseIndex(costumeNumber, costumeArray.length);
  }
  return costumeIndex;
};

// Convert base64 to raw binary data held in a dataURL.
// @see https://stackoverflow.com/questions/4998908/convert-data-uri-to-file-then-append-to-formdata
/**
 * Convert dataURL to binary data.
 * @param {string} dataURL - data to convert
 * @returns {Uint8Array} - binary data
 */
var dataURLToBinary = function dataURLToBinary(dataURL) {
  var byteString;
  if (dataURL.split(',')[0].indexOf('base64') >= 0) {
    byteString = atob(dataURL.split(',')[1]);
  } else {
    byteString = decodeURI(dataURL.split(',')[1]);
  }
  var data = new Uint8Array(byteString.length);
  for (var i = 0; i < byteString.length; i++) {
    data[i] = byteString.charCodeAt(i);
  }
  return data;
};

/**
 * Create SVG costume to target.
 * @param {string} svgData - SVG data string
 * @param {Runtime} runtime - runtime
 * @param {string} imageName - name of the costume
 * @returns {void}
 */
var createAndAddSvgCostume = function createAndAddSvgCostume(svgData, runtime, imageName) {
  var svgBytes = new TextEncoder().encode(svgData);
  var asset = runtime.storage.createAsset(runtime.storage.AssetType.ImageVector, runtime.storage.DataFormat.SVG, svgBytes, null, true // generate md5
  );
  var newCostume = {
    name: imageName,
    dataFormat: runtime.storage.DataFormat.SVG,
    asset: asset,
    md5: "".concat(asset.assetId, ".svg"),
    assetId: asset.assetId
  };

  // Create SVG skin
  newCostume.skinId = runtime.renderer.createSVGSkin(svgData);
  newCostume.size = runtime.renderer.getSkinSize(newCostume.skinId);
  var rotationCenter = runtime.renderer.getSkinRotationCenter(newCostume.skinId);
  newCostume.rotationCenterX = rotationCenter[0];
  newCostume.rotationCenterY = rotationCenter[1];
  newCostume.bitmapResolution = 1;
  return newCostume;
};

/**
 * Insert image as an SVG costume at the specified index.
 * @param {Runtime} runtime - runtime
 * @param {Target} target - target to add costume
 * @param {string} dataURL - image data
 * @param {number} width - desired width for the costume (optional, defaults to stage size)
 * @param {number} height - desired height for the costume (optional, defaults to stage size)
 * @param {string} imageName - name of the costume
 * @param {number} insertIndex - index to insert the costume (optional, 0-based, defaults to end of the list)
 * @returns {Promise} - a Promise that resolves when the image is added
*/
var insertImageAsSvgCostume = /*#__PURE__*/function () {
  var _ref2 = _asyncToGenerator(/*#__PURE__*/_regeneratorRuntime.mark(function _callee2(runtime, target, dataURL, width, height) {
    var imageName,
      insertIndex,
      mimeString,
      svgData,
      byteString,
      image,
      originalWidth,
      originalHeight,
      defaultStageWidth,
      defaultStageHeight,
      hasCustomWidth,
      hasCustomHeight,
      useCustomSize,
      svgViewportWidth,
      svgViewportHeight,
      stageScale,
      scale,
      scaledWidth,
      scaledHeight,
      translateX,
      translateY,
      formatNumber,
      svgAttributes,
      imageTransform,
      newCostume,
      currentCostumeIndex,
      finalCostumeIndex,
      willShiftCurrentCostume,
      costume,
      _args2 = arguments;
    return _regeneratorRuntime.wrap(function _callee2$(_context2) {
      while (1) switch (_context2.prev = _context2.next) {
        case 0:
          imageName = _args2.length > 5 && _args2[5] !== undefined ? _args2[5] : 'costume';
          insertIndex = _args2.length > 6 ? _args2[6] : undefined;
          mimeString = dataURL.split(',')[0].split(':')[1].split(';')[0];
          if (!(mimeString === 'image/svg+xml')) {
            _context2.next = 8;
            break;
          }
          // Convert base64 to raw binary data for SVG

          if (dataURL.split(',')[0].indexOf('base64') >= 0) {
            byteString = atob(dataURL.split(',')[1]);
          } else {
            byteString = decodeURI(dataURL.split(',')[1]);
          }
          svgData = byteString;
          _context2.next = 32;
          break;
        case 8:
          if (!(mimeString === 'image/jpeg' || mimeString === 'image/png')) {
            _context2.next = 31;
            break;
          }
          // Create an SVG wrapper for bitmap images
          image = new Image();
          _context2.next = 12;
          return new Promise(function (resolve, reject) {
            image.onload = resolve;
            image.onerror = function () {
              return reject(new Error('Costume load failed. Asset could not be read.'));
            };
            image.src = dataURL;
          });
        case 12:
          originalWidth = image.naturalWidth || image.width;
          originalHeight = image.naturalHeight || image.height;
          defaultStageWidth = 480;
          defaultStageHeight = 360;
          hasCustomWidth = typeof width === 'number' && !Number.isNaN(width) && width > 0;
          hasCustomHeight = typeof height === 'number' && !Number.isNaN(height) && height > 0;
          useCustomSize = hasCustomWidth && hasCustomHeight;
          if (useCustomSize) {
            svgViewportWidth = width;
            svgViewportHeight = height;
          } else {
            stageScale = Math.min(1, defaultStageWidth / originalWidth, defaultStageHeight / originalHeight);
            svgViewportWidth = Math.max(1, Math.round(originalWidth * stageScale));
            svgViewportHeight = Math.max(1, Math.round(originalHeight * stageScale));
          }
          scale = Math.min(svgViewportWidth / originalWidth, svgViewportHeight / originalHeight);
          scaledWidth = originalWidth * scale;
          scaledHeight = originalHeight * scale;
          translateX = (svgViewportWidth - scaledWidth) / 2;
          translateY = (svgViewportHeight - scaledHeight) / 2;
          formatNumber = function formatNumber(value) {
            return Number.isInteger(value) ? value : Number(value.toFixed(2));
          };
          svgAttributes = ['version="1.1"', 'xmlns="http://www.w3.org/2000/svg"', 'xmlns:xlink="http://www.w3.org/1999/xlink"', "width=\"".concat(formatNumber(svgViewportWidth), "\""), "height=\"".concat(formatNumber(svgViewportHeight), "\""), "viewBox=\"0 0 ".concat(formatNumber(svgViewportWidth), " ").concat(formatNumber(svgViewportHeight), "\"")].join(' ');
          imageTransform = ["translate(".concat(formatNumber(translateX), ",").concat(formatNumber(translateY), ")"), "scale(".concat(formatNumber(scale), ")")].join(' ');
          svgData = "<svg ".concat(svgAttributes, ">\n  <image width=\"").concat(formatNumber(originalWidth), "\" height=\"").concat(formatNumber(originalHeight), "\" ") + "transform=\"".concat(imageTransform, "\" xlink:href=\"").concat(dataURL, "\"/>\n</svg>");
          _context2.next = 32;
          break;
        case 31:
          throw new Error("Unsupported image type: ".concat(mimeString));
        case 32:
          newCostume = createAndAddSvgCostume(svgData, runtime, imageName);
          currentCostumeIndex = target.currentCostume;
          finalCostumeIndex = typeof insertIndex === 'number' ? insertIndex : target.getCostumes().length; // Check if insertion will change which costume the current index points to
          willShiftCurrentCostume = finalCostumeIndex <= currentCostumeIndex; // Add costume using the sprite's direct method to avoid triggering target events
          target.sprite.addCostumeAt(newCostume, finalCostumeIndex);

          // Update the current costume index directly without triggering setCostume
          if (willShiftCurrentCostume) {
            target.currentCostume = currentCostumeIndex + 1;
          }
          // If not shifted, the index remains correct

          // Force an immediate redraw to stabilize the renderer state
          if (runtime.renderer && target.drawableID !== null) {
            costume = target.getCostumes()[target.currentCostume];
            if (costume && costume.skinId !== null) {
              runtime.renderer.updateDrawableSkinId(target.drawableID, costume.skinId);
              // Immediately draw to ensure the renderer is in a stable state
              runtime.renderer.draw();
            }
          }
          runtime.emitProjectChanged();

          // Wait for multiple animation frames to ensure the costume is fully rendered
          // This prevents flickering when immediately switching to the newly added costume
          return _context2.abrupt("return", new Promise(function (resolve) {
            requestAnimationFrame(function () {
              requestAnimationFrame(function () {
                requestAnimationFrame(function () {
                  resolve(newCostume);
                });
              });
            });
          }));
        case 41:
        case "end":
          return _context2.stop();
      }
    }, _callee2);
  }));
  return function insertImageAsSvgCostume(_x4, _x5, _x6, _x7, _x8) {
    return _ref2.apply(this, arguments);
  };
}();

/**
 * Flip a costume horizontally or vertically.
 * @param {Runtime} runtime - runtime
 * @param {Target} target - target to flip costume
 * @param {string} costumeName - name or number of the costume to flip
 * @param {string} direction - 'horizontal' or 'vertical'
 * @returns {Promise<Costume>} - a Promise that resolves when the costume is flipped
 */
var flipCostume = /*#__PURE__*/function () {
  var _ref3 = _asyncToGenerator(/*#__PURE__*/_regeneratorRuntime.mark(function _callee3(runtime, target, costumeName, direction) {
    var costumeIndex, costume, asset, dataFormat, svgString, parser, svgDoc, svgElement, width, height, g, flippedSvgString, svgBytes, newAsset, rotationCenter, drawable, blob, imageElement, canvas, ctx, flippedDataURL, binaryData, _newAsset, newBlob, imageBitmap, tempCanvas, tempCtx, renderSize, _rotationCenter, _drawable;
    return _regeneratorRuntime.wrap(function _callee3$(_context3) {
      while (1) switch (_context3.prev = _context3.next) {
        case 0:
          costumeIndex = getCostumeIndexByNameOrNumber(target, costumeName);
          if (!(costumeIndex === null)) {
            _context3.next = 3;
            break;
          }
          throw new Error('Costume not found');
        case 3:
          costume = target.getCostumes()[costumeIndex];
          asset = costume.asset;
          dataFormat = costume.dataFormat; // Check if it's SVG or bitmap
          if (!(dataFormat === runtime.storage.DataFormat.SVG)) {
            _context3.next = 32;
            break;
          }
          // Handle SVG flip
          svgString = asset.decodeText();
          parser = new DOMParser();
          svgDoc = parser.parseFromString(svgString, 'image/svg+xml');
          svgElement = svgDoc.documentElement; // Get current dimensions
          width = parseFloat(svgElement.getAttribute('width')) || 100;
          height = parseFloat(svgElement.getAttribute('height')) || 100; // Create a transform group to flip the content
          g = svgDoc.createElementNS('http://www.w3.org/2000/svg', 'g');
          if (direction === 'horizontal') {
            // Flip horizontally: scale(-1, 1) and translate
            g.setAttribute('transform', "scale(-1, 1) translate(".concat(-width, ", 0)"));
          } else if (direction === 'vertical') {
            // Flip vertically: scale(1, -1) and translate
            g.setAttribute('transform', "scale(1, -1) translate(0, ".concat(-height, ")"));
          }

          // Move all children into the transform group
          while (svgElement.firstChild) {
            g.appendChild(svgElement.firstChild);
          }
          svgElement.appendChild(g);
          flippedSvgString = new XMLSerializer().serializeToString(svgDoc);
          svgBytes = new TextEncoder().encode(flippedSvgString); // Create new asset
          newAsset = runtime.storage.createAsset(runtime.storage.AssetType.ImageVector, runtime.storage.DataFormat.SVG, svgBytes, null, true); // Update costume with new asset
          costume.asset = newAsset;
          costume.md5 = "".concat(newAsset.assetId, ".svg");
          costume.assetId = newAsset.assetId;

          // Reload the skin
          if (costume.skinId) {
            runtime.renderer.destroySkin(costume.skinId);
          }
          costume.skinId = runtime.renderer.createSVGSkin(flippedSvgString);
          costume.size = runtime.renderer.getSkinSize(costume.skinId);
          rotationCenter = runtime.renderer.getSkinRotationCenter(costume.skinId);
          costume.rotationCenterX = rotationCenter[0];
          costume.rotationCenterY = rotationCenter[1];

          // Update the drawable if this is the current costume
          if (target.currentCostume === costumeIndex) {
            target.setCostume(costumeIndex);
            // Force update the drawable
            drawable = runtime.renderer._allDrawables[target.drawableID];
            if (drawable) {
              drawable.updateSkin(costume.skinId);
              runtime.requestRedraw();
            }
          }
          _context3.next = 66;
          break;
        case 32:
          // Handle bitmap flip
          blob = new Blob([asset.data], {
            type: asset.assetType.contentType
          });
          imageElement = new Image();
          imageElement.src = URL.createObjectURL(blob);
          _context3.next = 37;
          return new Promise(function (resolve, reject) {
            imageElement.onload = resolve;
            imageElement.onerror = function () {
              return reject(new Error('Image load failed'));
            };
          });
        case 37:
          canvas = document.createElement('canvas');
          canvas.width = imageElement.width;
          canvas.height = imageElement.height;
          ctx = canvas.getContext('2d'); // Flip the canvas
          if (direction === 'horizontal') {
            ctx.scale(-1, 1);
            ctx.drawImage(imageElement, -canvas.width, 0);
          } else if (direction === 'vertical') {
            ctx.scale(1, -1);
            ctx.drawImage(imageElement, 0, -canvas.height);
          }
          URL.revokeObjectURL(imageElement.src);

          // Convert canvas to data URL
          flippedDataURL = canvas.toDataURL("image/".concat(dataFormat));
          binaryData = dataURLToBinary(flippedDataURL); // Create new asset
          _newAsset = runtime.storage.createAsset(runtime.storage.AssetType.ImageBitmap, dataFormat, binaryData, null, true); // Update costume with new asset
          costume.asset = _newAsset;
          costume.md5 = "".concat(_newAsset.assetId, ".").concat(dataFormat);
          costume.assetId = _newAsset.assetId;

          // Reload the skin
          if (costume.skinId) {
            runtime.renderer.destroySkin(costume.skinId);
          }
          newBlob = new Blob([_newAsset.data], {
            type: _newAsset.assetType.contentType
          });
          _context3.next = 53;
          return createImageBitmap(newBlob);
        case 53:
          imageBitmap = _context3.sent;
          tempCanvas = document.createElement('canvas');
          tempCanvas.width = imageBitmap.width;
          tempCanvas.height = imageBitmap.height;
          tempCtx = tempCanvas.getContext('2d');
          tempCtx.drawImage(imageBitmap, 0, 0);
          costume.skinId = runtime.renderer.createBitmapSkin(tempCanvas, costume.bitmapResolution);
          renderSize = runtime.renderer.getSkinSize(costume.skinId);
          costume.size = [renderSize[0] * costume.bitmapResolution, renderSize[1] * costume.bitmapResolution];
          _rotationCenter = runtime.renderer.getSkinRotationCenter(costume.skinId);
          costume.rotationCenterX = _rotationCenter[0] * costume.bitmapResolution;
          costume.rotationCenterY = _rotationCenter[1] * costume.bitmapResolution;

          // Update the drawable if this is the current costume
          if (target.currentCostume === costumeIndex) {
            target.setCostume(costumeIndex);
            // Force update the drawable
            _drawable = runtime.renderer._allDrawables[target.drawableID];
            if (_drawable) {
              _drawable.updateSkin(costume.skinId);
              runtime.requestRedraw();
            }
          }
        case 66:
          runtime.emitProjectChanged();
          return _context3.abrupt("return", costume);
        case 68:
        case "end":
          return _context3.stop();
      }
    }, _callee3);
  }));
  return function flipCostume(_x9, _x0, _x1, _x10) {
    return _ref3.apply(this, arguments);
  };
}();

/**
 * Formatter which is used for translation.
 * This will be replaced which is used in the runtime.
 * @param {object} messageData - format-message object
 * @returns {string} - message for the locale
 */
var formatMessage = function formatMessage(messageData) {
  return messageData.default;
};

/**
 * Setup format-message for this extension.
 */
var setupTranslations = function setupTranslations() {
  var localeSetup = formatMessage.setup();
  if (localeSetup && localeSetup.translations[localeSetup.locale]) {
    Object.assign(localeSetup.translations[localeSetup.locale], translations[localeSetup.locale]);
  }
};
var EXTENSION_ID = 'costumex';

/**
 * URL to get this extension as a module.
 * When it was loaded as a module, 'extensionURL' will be replaced a URL which is retrieved from.
 * @type {string}
 */
var extensionURL = 'https://yokobond.github.io/xcx-costumex/dist/costumex.mjs';

/**
 * Scratch 3.0 blocks for example of Xcratch.
 */
var ExtensionBlocks = /*#__PURE__*/function () {
  /**
   * Construct a set of blocks for CostumeX.
   * @param {Runtime} runtime - the Scratch 3.0 runtime.
   */
  function ExtensionBlocks(runtime) {
    _classCallCheck$1(this, ExtensionBlocks);
    /**
     * The Scratch 3.0 runtime.
     * @type {Runtime}
     */
    this.runtime = runtime;
    if (runtime.formatMessage) {
      // Replace 'formatMessage' to a formatter which is used in the runtime.
      formatMessage = runtime.formatMessage;
    }
    runtime.on('EXTENSION_ADDED', this.onExtensionAdded.bind(this));
  }
  _createClass$1(ExtensionBlocks, [{
    key: "onExtensionAdded",
    value: function onExtensionAdded(extensionInfo) {
      if (extensionInfo.id === EXTENSION_ID) {
        checkDebugMode();
      }
    }

    /**
     * @returns {object} metadata for this extension and its blocks.
     */
  }, {
    key: "getInfo",
    value: function getInfo() {
      setupTranslations();
      return {
        id: ExtensionBlocks.EXTENSION_ID,
        name: ExtensionBlocks.EXTENSION_NAME,
        extensionURL: ExtensionBlocks.extensionURL,
        blockIconURI: img,
        showStatusButton: false,
        blocks: [{
          opcode: 'insertSnapshotAsCostume',
          blockType: BlockType$1.COMMAND,
          text: formatMessage({
            id: 'costumex.insertSnapshotAsCostume',
            default: 'insert snapshot [NAME] at [INDEX] x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]',
            description: 'costumex insertSnapshotAsCostume text'
          }),
          func: 'insertSnapshotAsCostume',
          arguments: {
            NAME: {
              type: ArgumentType$1.STRING,
              defaultValue: formatMessage({
                id: 'costumex.insertSnapshotAsCostume.defaultCostumeName',
                default: 'snapshot',
                description: 'CostumeX insertSnapshotAsCostume defaultCostumeName text'
              })
            },
            INDEX: {
              type: ArgumentType$1.STRING,
              defaultValue: ' '
            },
            X: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            Y: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            WIDTH: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 480
            },
            HEIGHT: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 360
            }
          }
        }, {
          opcode: 'insertCameraImageAsCostume',
          blockType: BlockType$1.COMMAND,
          text: formatMessage({
            id: 'costumex.insertCameraImageAsCostume',
            default: 'insert video frame [NAME] at [INDEX] x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]',
            description: 'costumex insertCameraImageAsCostume text'
          }),
          func: 'insertCameraImageAsCostume',
          arguments: {
            NAME: {
              type: ArgumentType$1.STRING,
              defaultValue: formatMessage({
                id: 'costumex.insertCameraImageAsCostume.defaultCostumeName',
                default: 'video',
                description: 'CostumeX insertCameraImageAsCostume defaultCostumeName text'
              })
            },
            INDEX: {
              type: ArgumentType$1.STRING,
              defaultValue: ' '
            },
            X: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            Y: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            WIDTH: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 480
            },
            HEIGHT: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 360
            }
          }
        }, {
          opcode: 'insertImageAsCostume',
          blockType: BlockType$1.COMMAND,
          text: formatMessage({
            id: 'costumex.insertImageAsCostume',
            default: 'insert costume [NAME] at [INDEX] width [WIDTH] height [HEIGHT] with image [DATA]',
            description: 'CostumeX insertImageAsCostume text'
          }),
          func: 'insertImageAsCostume',
          arguments: {
            DATA: {
              type: ArgumentType$1.STRING,
              defaultValue: 'data:image/png;base64,AAA'
            },
            NAME: {
              type: ArgumentType$1.STRING,
              defaultValue: formatMessage({
                id: 'costumex.insertImageAsCostume.defaultCostumeName',
                default: 'costume',
                description: 'CostumeX insertImageAsCostume defaultCostumeName text'
              })
            },
            INDEX: {
              type: ArgumentType$1.STRING,
              defaultValue: ' '
            },
            WIDTH: {
              type: ArgumentType$1.STRING,
              defaultValue: ' '
            },
            HEIGHT: {
              type: ArgumentType$1.STRING,
              defaultValue: ' '
            }
          }
        }, {
          opcode: 'addImageAsCostume',
          hideFromPalette: true,
          blockType: BlockType$1.COMMAND,
          text: formatMessage({
            id: 'costumex.addImageAsCostume',
            default: 'add image [DATA] as costume [NAME]',
            description: 'CostumeX addImageAsCostume text'
          }),
          func: 'addImageAsCostume',
          arguments: {
            DATA: {
              type: ArgumentType$1.STRING,
              defaultValue: 'data:image/png;base64,'
            },
            NAME: {
              type: ArgumentType$1.STRING,
              defaultValue: formatMessage({
                id: 'costumex.addImageAsCostume.defaultCostumeName',
                default: 'costume',
                description: 'CostumeX addImageAsCostume defaultCostumeName text'
              })
            }
          }
        }, {
          opcode: 'flipCostume',
          blockType: BlockType$1.COMMAND,
          text: formatMessage({
            id: 'costumex.flipCostume',
            default: 'flip costume [COSTUME] [DIRECTION]',
            description: 'CostumeX flipCostume text'
          }),
          func: 'flipCostumeBlock',
          arguments: {
            COSTUME: {
              type: ArgumentType$1.COSTUME,
              menu: 'costumeNamesMenu'
            },
            DIRECTION: {
              type: ArgumentType$1.STRING,
              menu: 'flipDirectionMenu'
            }
          }
        }, {
          opcode: 'deleteCostume',
          blockType: BlockType$1.COMMAND,
          text: formatMessage({
            id: 'costumex.deleteCostume',
            default: 'delete costume [COSTUME]',
            description: 'CostumeX deleteCostume text'
          }),
          func: 'deleteCostume',
          arguments: {
            COSTUME: {
              type: ArgumentType$1.COSTUME,
              menu: 'costumeNamesMenu'
            }
          }
        }, {
          opcode: 'takeSnapshot',
          hideFromPalette: true,
          // Use insertSnapshotAsCostume instead.
          blockType: BlockType$1.REPORTER,
          blockAllThreads: false,
          text: formatMessage({
            id: 'costumex.takeSnapshot',
            default: 'snapshot center x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]',
            description: 'costumex takeSnapshot text'
          }),
          func: 'takeSnapshot',
          arguments: {
            X: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            Y: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            WIDTH: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 480
            },
            HEIGHT: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 360
            }
          }
        }, {
          opcode: 'captureVideoFrame',
          hideFromPalette: true,
          // Use insertCameraImageAsCostume instead.
          blockType: BlockType$1.REPORTER,
          blockAllThreads: false,
          text: formatMessage({
            id: 'costumex.captureVideoFrame',
            default: 'capture video center x:[X] y:[Y] w:[WIDTH] h:[HEIGHT]',
            description: 'costumex captureVideoFrame text'
          }),
          func: 'captureVideoFrame',
          arguments: {
            X: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            Y: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 0
            },
            WIDTH: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 480
            },
            HEIGHT: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 360
            }
          }
        }, {
          opcode: 'costumeData',
          blockType: BlockType$1.REPORTER,
          text: formatMessage({
            id: 'costumex.costumeData',
            default: 'image data of costume [COSTUME]',
            description: 'CostumeX costumeData text'
          }),
          func: 'costumeData',
          arguments: {
            COSTUME: {
              type: ArgumentType$1.COSTUME,
              menu: 'costumeNamesMenu'
            }
          }
        }, {
          opcode: 'costumeSize',
          blockType: BlockType$1.REPORTER,
          text: formatMessage({
            id: 'costumex.costumeSize',
            default: '[DIMENSION] of costume [COSTUME]',
            description: 'CostumeX costumeSize text'
          }),
          func: 'costumeSize',
          arguments: {
            DIMENSION: {
              type: ArgumentType$1.STRING,
              menu: 'dimensionMenu'
            },
            COSTUME: {
              type: ArgumentType$1.COSTUME,
              menu: 'costumeNamesMenu'
            }
          }
        }, {
          opcode: 'costumesLength',
          blockType: BlockType$1.REPORTER,
          disableMonitor: true,
          text: formatMessage({
            id: 'costumex.costumesLength',
            default: 'costumes length',
            description: 'CostumeX costumesLength text'
          }),
          func: 'costumesLength'
        }, {
          opcode: 'costumeNameAt',
          blockType: BlockType$1.REPORTER,
          text: formatMessage({
            id: 'costumex.costumeNameAt',
            default: 'costume name at [INDEX]',
            description: 'CostumeX costumeNameAt text'
          }),
          func: 'costumeNameAt',
          arguments: {
            INDEX: {
              type: ArgumentType$1.NUMBER,
              defaultValue: 1
            }
          }
        }],
        menus: {
          costumeNamesMenu: {
            acceptReporters: true,
            items: 'getCostumeNamesMenu'
          },
          dimensionMenu: {
            items: [{
              text: formatMessage({
                id: 'costumex.dimensionMenu.width',
                default: 'width',
                description: 'CostumeX width text'
              }),
              value: 'width'
            }, {
              text: formatMessage({
                id: 'costumex.dimensionMenu.height',
                default: 'height',
                description: 'CostumeX height text'
              }),
              value: 'height'
            }, {
              text: formatMessage({
                id: 'costumex.dimensionMenu.centerX',
                default: 'center x'
              }),
              value: 'centerX'
            }, {
              text: formatMessage({
                id: 'costumex.dimensionMenu.centerY',
                default: 'center y'
              }),
              value: 'centerY'
            }]
          },
          flipDirectionMenu: {
            items: [{
              text: formatMessage({
                id: 'costumex.flipDirectionMenu.horizontal',
                default: 'left-right',
                description: 'CostumeX horizontal flip'
              }),
              value: 'horizontal'
            }, {
              text: formatMessage({
                id: 'costumex.flipDirectionMenu.vertical',
                default: 'up-down',
                description: 'CostumeX vertical flip'
              }),
              value: 'vertical'
            }]
          }
        }
      };
    }
  }, {
    key: "getCostumeNamesMenu",
    value: function getCostumeNamesMenu() {
      var menu = [];
      var target = this.runtime.getEditingTarget();
      if (!target) {
        return menu;
      }
      var costumes = target.sprite.costumes;
      for (var i = 0; i < costumes.length; i++) {
        menu.push({
          text: costumes[i].name,
          value: costumes[i].name
        });
      }
      return menu;
    }

    /**
     * Take a snapshot.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {Promise<string>} - a Promise that resolves snapshot data URL
     */
  }, {
    key: "takeSnapshot",
    value: function takeSnapshot(args, util) {
      var _this = this;
      var centerX = Cast$1.toNumber(args.X);
      var centerY = Cast$1.toNumber(args.Y);
      var width = Cast$1.toNumber(args.WIDTH);
      var height = Cast$1.toNumber(args.HEIGHT);
      var runtime = util.runtime;
      return new Promise(function (resolve) {
        _this.runtime.renderer.requestSnapshot(function (imageDataURL) {
          resolve(" ".concat(imageDataURL, " "));
        });
      }).then(function (imageDataUrl) {
        return new Promise(function (resolve, reject) {
          var image = new Image();
          image.onload = function () {
            resolve(image);
            image.onload = null;
            image.onerror = null;
          };
          image.onerror = function () {
            reject(new Error('Costume load failed. Asset could not be read.'));
            image.onload = null;
            image.onerror = null;
          };
          image.src = imageDataUrl;
        });
      }).then(function (imageElem) {
        var bitmapResolution = 2;
        var outputWidth = 480 * bitmapResolution;
        var outputHeight = 360 * bitmapResolution;
        var _runtime$renderer$get = runtime.renderer.getNativeSize(),
          _runtime$renderer$get2 = _slicedToArray(_runtime$renderer$get, 2),
          stageWidth = _runtime$renderer$get2[0],
          stageHeight = _runtime$renderer$get2[1];
        var stepPixelRatioWidth = imageElem.width / stageWidth;
        var stepPixelRatioHeight = imageElem.height / stageHeight;
        var canvas = document.createElement('canvas');
        canvas.width = width / stageWidth * outputWidth;
        canvas.height = height / stageHeight * outputHeight;
        var context = canvas.getContext('2d');
        context.drawImage(imageElem, (stageWidth / 2 + centerX - width / 2) * stepPixelRatioWidth, (stageHeight / 2 - (centerY + height / 2)) * stepPixelRatioHeight, width * stepPixelRatioWidth, height * stepPixelRatioHeight, 0, 0, canvas.width, canvas.height);
        return " ".concat(canvas.toDataURL(), " ");
      }).catch(function (error) {
        log$1.error(error);
        return error.message;
      });
    }

    /**
     * Insert a snapshot as a costume.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {Promise<string>} - a Promise that resolves when the snapshot is added then returns the data URL
     */
  }, {
    key: "insertSnapshotAsCostume",
    value: function insertSnapshotAsCostume(args, util) {
      var target = util.target;
      var imageName = Cast$1.toString(args.NAME);
      var runtime = this.runtime;
      var insertIndex = Cast$1.toNumber(args.INDEX);
      if (isNaN(insertIndex) || insertIndex < 1) {
        insertIndex = 1;
      }
      if (insertIndex > target.getCostumes().length + 1) {
        insertIndex = target.getCostumes().length + 1;
      }
      insertIndex -= 1; // Convert to 0-origin

      var width = Cast$1.toNumber(args.WIDTH);
      var height = Cast$1.toNumber(args.HEIGHT);
      return this.takeSnapshot(args, util).then(function (dataURL) {
        var trimmedDataURL = dataURL.trim();
        return insertImageAsSvgCostume(runtime, target, trimmedDataURL, width, height, imageName, insertIndex);
      }).then(function (costume) {
        return " ".concat(costume.asset.encodeDataURI(), " ");
      }).catch(function (error) {
        log$1.error(error);
        return error.message;
      });
    }

    /**
     * Insert a video frame as a costume.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {Promise<string>} - a Promise that resolves when the video frame is added then returns the data URL
     */
  }, {
    key: "insertCameraImageAsCostume",
    value: function insertCameraImageAsCostume(args, util) {
      var target = util.target;
      var imageName = Cast$1.toString(args.NAME);
      var runtime = this.runtime;
      var insertIndex = Cast$1.toNumber(args.INDEX);
      if (isNaN(insertIndex) || insertIndex < 1) {
        insertIndex = 1;
      }
      if (insertIndex > target.getCostumes().length + 1) {
        insertIndex = target.getCostumes().length + 1;
      }
      insertIndex -= 1; // Convert to 0-origin

      var width = Cast$1.toNumber(args.WIDTH);
      var height = Cast$1.toNumber(args.HEIGHT);
      return this.captureVideoFrame(args, util).then(function (dataURL) {
        var trimmedDataURL = dataURL.trim();
        return insertImageAsSvgCostume(runtime, target, trimmedDataURL, width, height, imageName, insertIndex);
      }).then(function (costume) {
        return " ".concat(costume.asset.encodeDataURI(), " ");
      }).catch(function (error) {
        log$1.error(error);
        return error.message;
      });
    }

    /**
     * Capture a video frame.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {Promise<string>} - a Promise that resolves video frame data URL
     */
  }, {
    key: "captureVideoFrame",
    value: function captureVideoFrame(args, util) {
      var centerX = Cast$1.toNumber(args.X);
      var centerY = Cast$1.toNumber(args.Y);
      var width = Cast$1.toNumber(args.WIDTH);
      var height = Cast$1.toNumber(args.HEIGHT);
      var runtime = util.runtime;
      var captureResolution = 2;
      var videoProvider = runtime.ioDevices.video.provider;
      if (!videoProvider) {
        return Promise.resolve('');
      }
      return videoProvider.enableVideo().then(function () {
        return (
          // Wait for video to be ready
          new Promise(function (resolve) {
            var checkVideoReady = function checkVideoReady() {
              if (videoProvider.videoReady) {
                resolve();
              } else {
                setTimeout(checkVideoReady, 100);
              }
            };
            checkVideoReady();
          })
        );
      }).then(function () {
        var sourceCanvas = videoProvider.getFrame({
          dimensions: [480 * captureResolution, 360 * captureResolution],
          format: 'canvas'
        });
        if (!sourceCanvas) {
          return '';
        }

        // Get actual canvas dimensions from video provider
        var canvasWidth = sourceCanvas.width;
        var canvasHeight = sourceCanvas.height;
        var videoWidth = 480,
          videoHeight = 360;

        // Calculate scale factor from video dimensions to canvas dimensions
        var scaleX = canvasWidth / videoWidth;
        var scaleY = canvasHeight / videoHeight;

        // Calculate source rectangle in canvas coordinates (high resolution)
        var srcX = (videoWidth / 2 + centerX - width / 2) * scaleX;
        var srcY = (videoHeight / 2 - (centerY + height / 2)) * scaleY;
        var srcWidth = width * scaleX;
        var srcHeight = height * scaleY;

        // Create output canvas with high resolution matching source
        var outputCanvas = document.createElement('canvas');
        outputCanvas.width = srcWidth;
        outputCanvas.height = srcHeight;
        var context = outputCanvas.getContext('2d');

        // Draw the cropped region to output canvas at full resolution
        context.drawImage(sourceCanvas, srcX, srcY, srcWidth, srcHeight, 0, 0, srcWidth, srcHeight);
        return " ".concat(outputCanvas.toDataURL(), " ");
      }).catch(function (error) {
        log$1.error(error);
        return error.message;
      });
    }

    /**
     * Insert an image as a costume.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {Promise<string>} - a Promise that resolves when the image is added then returns the data URL
     */
  }, {
    key: "insertImageAsCostume",
    value: function insertImageAsCostume(args, util) {
      var target = util.target;
      var dataURL = Cast$1.toString(args.DATA).trim();
      var imageName = Cast$1.toString(args.NAME);
      var runtime = this.runtime;
      var insertIndex = Cast$1.toNumber(args.INDEX);
      if (isNaN(insertIndex) || insertIndex < 1) {
        insertIndex = 1;
      }
      if (insertIndex > target.getCostumes().length + 1) {
        insertIndex = target.getCostumes().length + 1;
      }
      insertIndex -= 1; // Convert to 0-origin
      var width = Cast$1.toNumber(args.WIDTH);
      if (isNaN(width) || width <= 0) {
        width = runtime.renderer.canvas.width;
      }
      var height = Cast$1.toNumber(args.HEIGHT);
      if (isNaN(height) || height <= 0) {
        height = runtime.renderer.canvas.height;
      }
      return insertImageAsSvgCostume(runtime, target, dataURL, width, height, imageName, insertIndex).then(function (costume) {
        return " ".concat(costume.asset.encodeDataURI(), " ");
      }).catch(function (error) {
        log$1.error(error);
        return error.message;
      });
    }

    /**
     * Add an image as a costume.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {Promise<string>} - a Promise that resolves when the image is added then returns the data URL
     * @deprecated Use 'insertImageAsCostume' instead.
     */
  }, {
    key: "addImageAsCostume",
    value: function addImageAsCostume(args, util) {
      return this.insertImageAsCostume(args, util);
    }

    /**
     * Delete a costume.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "deleteCostume",
    value: function deleteCostume(args, util) {
      var target = util.target;
      if (target.sprite.costumes.length <= 1) {
        // Must have at least one costume
        return;
      }
      var costumeName = Cast$1.toString(args.COSTUME);
      var costumeIndex = getCostumeIndexByNameOrNumber(target, costumeName);
      if (costumeIndex === null) return;
      target.deleteCostume(costumeIndex);
    }

    /**
     * Get the number of costumes.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {number} - the number of costumes.
     */
  }, {
    key: "costumesLength",
    value: function costumesLength(args, util) {
      return util.target.sprite.costumes.length;
    }

    /**
     * Get the name of a costume at a given index.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {string} - the name of the costume.
     */
  }, {
    key: "costumeNameAt",
    value: function costumeNameAt(args, util) {
      var index = Cast$1.toNumber(args.INDEX);
      var target = util.target;
      var costumes = target.sprite.costumes;
      if (index < 1 || index > costumes.length) {
        return '';
      }
      return costumes[index - 1].name;
    }

    /**
     * Get the image data of a costume.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {string} - the image data of the costume.
     */
  }, {
    key: "costumeData",
    value: function costumeData(args, util) {
      var target = util.target;
      var costumeName = Cast$1.toString(args.COSTUME);
      var costumeIndex = getCostumeIndexByNameOrNumber(target, costumeName);
      if (costumeIndex === null) return '';
      return " ".concat(target.getCostumes()[costumeIndex].asset.encodeDataURI(), " ");
    }
  }, {
    key: "costumeSize",
    value: function costumeSize(args, util) {
      var dimension = Cast$1.toString(args.DIMENSION).toLowerCase();
      var target = util.target;
      var costumeName = Cast$1.toString(args.COSTUME);
      var costumeIndex = getCostumeIndexByNameOrNumber(target, costumeName);
      if (costumeIndex === null) return 0;
      var costume = target.getCostumes()[costumeIndex];
      // Get sprite size (scale factor) as a percentage (e.g., 100 means 100%)
      var spriteScale = target.size / 100;
      var resolution = costume.bitmapResolution;
      if (dimension === 'width') {
        return Math.round(resolution * costume.size[0] * spriteScale);
      }
      if (dimension === 'height') {
        return Math.round(resolution * costume.size[1] * spriteScale);
      }
      if (dimension === 'centerx') {
        var centerX = costume.size[0] / 2 - costume.rotationCenterX;
        return Math.round(resolution * centerX * spriteScale);
      }
      if (dimension === 'centery') {
        var centerY = costume.rotationCenterY - costume.size[1] / 2;
        return Math.round(resolution * centerY * spriteScale);
      }
      return 0;
    }

    /**
     * Flip a costume.
     * @param {object} args - the block's arguments.
     * @param {object} util - utility object provided by the runtime.
     * @returns {Promise} - a Promise that resolves when the costume is flipped
     */
  }, {
    key: "flipCostumeBlock",
    value: function flipCostumeBlock(args, util) {
      var target = util.target;
      var costumeName = Cast$1.toString(args.COSTUME);
      var direction = Cast$1.toString(args.DIRECTION);
      var runtime = this.runtime;
      return flipCostume(runtime, target, costumeName, direction).then(function () {
        return Promise.resolve();
      }).catch(function (error) {
        log$1.error(error);
      });
    }
  }], [{
    key: "formatMessage",
    set:
    /**
     * A translation object which is used in this class.
     * @param {FormatObject} formatter - translation object
     */
    function set(formatter) {
      formatMessage = formatter;
      if (formatMessage) setupTranslations();
    }

    /**
     * @return {string} - the name of this extension.
     */
  }, {
    key: "EXTENSION_NAME",
    get: function get() {
      return formatMessage({
        id: 'costumex.name',
        default: 'CostumeX',
        description: 'name of the extension'
      });
    }

    /**
     * @return {string} - the ID of this extension.
     */
  }, {
    key: "EXTENSION_ID",
    get: function get() {
      return EXTENSION_ID;
    }

    /**
     * URL to get this extension.
     * @type {string}
     */
  }, {
    key: "extensionURL",
    get: function get() {
      return extensionURL;
    }

    /**
     * Set URL to get this extension.
     * The extensionURL will be changed to the URL of the loading server.
     * @param {string} url - URL
     */,
    set: function set(url) {
      extensionURL = url;
    }
  }]);
  return ExtensionBlocks;
}();


/***/ })

}]);
//# sourceMappingURL=preload_https_3A_2F_2Fyokobond_2Egithub_2Eio_2Fxcx-costumex_2Fdist_2Fcostumex_2Emjs_extension_mjs.60110663e7696594a03c.js.map